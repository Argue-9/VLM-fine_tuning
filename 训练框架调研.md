# Qwen3-VL-8B 多模态 SFT→GRPO 训练框架调研

> 调研基准日：2026-08-05  
> 项目约束：Qwen3-VL-8B-Instruct；约 1 万样本；输入包含图片和动态 GIF；输出按 `description`、`evidence`、`risk_type`、`cot` 的固定顺序生成；先 SFT、再 GRPO；R5 规则根据 `risk_type + evidence` 计算奖励；FP16 LoRA 为主、QLoRA 仅作显存兜底；8×V100-SXM2-16GB；冻结视觉编码器、merger/projector、embedding 和 `lm_head`；稳定与不 OOM 优先。

## 1. 执行结论

本项目**不应把“裸 PEFT + TRL”直接作为默认工程框架**。建议如下：

1. **主选：MS-SWIFT**
   - SFT 和 GRPO 均使用 MS-SWIFT，底层仍使用 Transformers、PEFT、TRL/DeepSpeed 等组件。
   - 它是本次候选中，官方资料对 **Qwen3-VL、多模态 SFT、多模态 GRPO、LoRA、SFT adapter 接续 GRPO、KL、自定义 reward、DeepSpeed ZeRO-3** 覆盖最完整的一体化方案。
   - 首版采用 **FP16 LoRA + 8卡 DeepSpeed ZeRO-3 + Transformers 原地 rollout**；关闭 vLLM，显式关闭 generation 前完整模型 gather；attention 使用 SDPA，若不兼容则退回 eager。
   - R5 封装为 MS-SWIFT 自定义 reward plugin；GIF 离线确定性解帧后，以 Qwen3-VL 的单个 video 帧列表输入，而不是四张独立 image。

2. **备选：原生 Hugging Face 栈**
   - 组合为 Transformers + TRL + PEFT + Accelerate/DeepSpeed。
   - 官方已有 Qwen3-VL 的 SFT/GRPO 示例、VLM 数据接口、任意 Python reward、KL 与 PEFT 接口，能力上可完成任务。
   - 缺点是需要自行维护多模态预处理、模板一致性、adapter/reference 模型装载、reward 契约、分布式配置与版本兼容，工程风险和代码量明显高于 MS-SWIFT。

3. **当前不选：veRL、OpenRLHF、LLaMA-Factory、Qwen 官方 qwen-vl-finetune**
   - veRL/OpenRLHF 更依赖高性能 rollout 引擎，而本项目 V100 与当前 Qwen3-VL 所需 vLLM 版本没有已证实的官方兼容交集。
   - LLaMA-Factory 与 qwen-vl-finetune 可完成 SFT，但没有覆盖本项目所需的原生多模态 GRPO 全链路。

这是一项**有条件的框架决定**：官方没有给出“Qwen3-VL-8B + FP16 LoRA + Transformers GRPO + 8×V100 16GB”的峰值显存或成功案例。最终开训前，必须通过本文第 8 节的最小 smoke test；若仍 OOM，先确认视觉上限和异常样本、收缩 completion、调整 ZeRO bucket，再启用 CPU parameter offload，最后才切换 QLoRA。已确认的 GIF 最多四帧语义不能作为脚本内部的静默降级项。

## 2. 硬门槛与淘汰表

符号：✅ 官方证据明确覆盖；⚠️ 可行但有条件或必须实测；❓ 未找到官方证据；❌ 不满足硬门槛。

| 框架 | Qwen3-VL | 多模态 SFT | 多模态 GRPO | LoRA/SFT adapter 接续 | 自定义 reward + KL | 8×V100 路径 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| **MS-SWIFT** | ✅ | ✅ 图片/多图/视频 | ✅ 官方多模态 GRPO | ✅ `adapters` + `ref_adapters` | ✅ plugin reward；`beta` 控制 KL | ⚠️ V100/FP16/ZeRO-3 有组件级支持，但 8B GRPO 峰值未证实 | **主选** |
| **HF TRL + Transformers + PEFT + DeepSpeed** | ✅ 官方 SFT/GRPO notebook | ✅ | ✅ | ✅ | ✅ Python reward；`beta>0` 启用 KL | ⚠️ 可用 Transformers generation + ZeRO-3，需大量自建与实测 | **备选** |
| **veRL** | ⚠️ 近期代码/发布记录出现 Qwen3-VL | ✅ | ✅ 多模态 GRPO | ⚠️ 有 LoRA 能力 | ✅ | ❓ 典型 vLLM/SGLang rollout 与 V100 冲突；HF rollout 的 Qwen3-VL+V100 路径未证实 | 暂缓 |
| **OpenRLHF** | ❓ 官方仍有“Support Qwen3vl GRPO?”开放 issue | ⚠️ 新版 VLM 能力 | ❓ Qwen3-VL 路径未确认 | ⚠️ | ✅ | ❌ 当前架构主要依赖 Ray+vLLM，V100 无受支持路径 | 淘汰 |
| **LLaMA-Factory** | ✅ | ✅ 图片/视频 | ❌ 官方主框架无原生 GRPO trainer/reward API | ✅ SFT LoRA | ❌ | ⚠️ SFT 可尝试 FP16+ZeRO-3 | 淘汰：不能单框架完成 SFT→GRPO |
| **Qwen qwen-vl-finetune** | ✅ 官方代码 | ✅ 图片/多图/视频 | ❌ | ✅ SFT LoRA | ❌ | ⚠️ 官方默认依赖/示例偏 FA2、BF16，V100 需修改且无官方实测 | 淘汰：只适合 SFT/参考实现 |

淘汰的核心不是“框架不好”，而是是否同时满足本项目的硬组合：**Qwen3-VL-8B + 多模态 + SFT→GRPO + 自定义 R5 reward + KL + LoRA adapter 连续性 + V100 可落地路径**。

## 3. 主选：MS-SWIFT

### 3.1 为什么最适合本项目

MS-SWIFT 官方文档直接列出 Qwen3-VL，并提供 Qwen3-VL 最佳实践、多模态 GRPO、LoRA、DeepSpeed ZeRO-3、自定义 reward plugin，以及对 ViT/aligner/LLM 的独立冻结控制。相较手工拼接 TRL，它已经定义了多模态模板、dataset schema、adapter 载入、GRPO 参数和 reward 插件契约，能显著减少本项目最容易出错的 glue code。

与本项目需求的具体对应如下：

- **冻结要求**：使用 `freeze_vit=true`、`freeze_aligner=true`，并用精确 `target_regex` 只把 LoRA 注入语言模型指定线性层。`modules_to_save=[]`，避免把 embedding、`lm_head` 或其他冻结模块加入训练。
- **SFT→GRPO 连续性**：GRPO 从 SFT LoRA checkpoint 启动时，官方参数说明要求同时设置 `--adapters sft_ckpt --ref_adapters sft_ckpt`。若是续跑 GRPO，则用 `--resume_from_checkpoint rlhf_ckpt --ref_adapters sft_ckpt`。
- **KL reference**：设置非零 `beta`；禁止因默认值或配置遗漏而意外关闭 KL。reference adapter 的实际显存占用和 ZeRO-3 行为仍须 smoke test 核实。
- **R5 reward**：实现一个本地自定义 reward plugin。插件解析 completion 的严格 JSON，读取 `risk_type` 与 `evidence`，调用确定性的 R5 规则，并分别返回格式奖励、字段一致性奖励、R5 任务奖励。`description` 和 `cot` 可以参与格式/一致性验证，但二元结果不由 VLM 直接输出。
- **分布式**：首选 DeepSpeed ZeRO-3。理由不是 FSDP 不可用，而是官方 Qwen3-VL 多模态 GRPO 示例对 DeepSpeed 的覆盖更直接；“Qwen3-VL-8B + FSDP2 + V100”没有官方实测证据。
- **媒体动态分辨率**：静态图以单个 image 输入，`IMAGE_MAX_TOKEN_NUM=1024`（等价 `max_pixels=1048576`）；GIF 离线确定性解帧后，按 `videos: [[frame1, frame2, ...]]` 或等价的单个 video 帧列表格式输入，`VIDEO_MAX_TOKEN_NUM=256`/帧（等价 `max_pixels=262144`/帧）。统一 processor 只做确定性等比例缩放，不裁剪或改变宽高比。

### 3.2 V100 上的建议起点

首版配置原则：

- `torch_dtype=float16`，不使用 BF16；启用动态 loss scaling，并监测 NaN/Inf。
- `attn_impl=sdpa`；若当前 PyTorch/Qwen3-VL 组合不支持，再退回 `eager`。
- `use_vllm=false`，走 Transformers generation。
- DeepSpeed ZeRO-3；GRPO 固定 `ds3_gather_for_generation=false`，避免生成前把完整 8B 模型聚合到单张 16GB GPU。该选项会变慢，且不得与 vLLM 同用。
- per-device micro-batch 从 1 开始；开启 gradient checkpointing。
- 首轮固定 `num_generations=4`、`generation_batch_size=8`；若 smoke test 不能稳定运行，再把前者降为 `2`，但 8 rank 下最小 generation batch 仍为8。GRPO rollout 使用 `do_sample=true`、`temperature=0.9`、`top_p=0.95`、`top_k=-1`、`num_beams=1`、`repetition_penalty=1.0`。
- SFT/GRPO 均启用 `group_by_length=true`，长度来自包含视觉 token 的真实 processor 编码；SFT 与 GRPO 分别使用 target-inclusive 与 prompt-only length cache。保持 shuffle，禁用 packing 与 padding-free。
- GRPO `max_completion_length=512` 作为首轮上限；正式 prompt/SFT 总长度和 completion P95/P99 在 taxonomy 渲染、参考数据完成后确定。所有阶段使用 `truncation_strategy=delete`，不静默截断 taxonomy、视觉 token 或 JSON。
- 暂不启用 `torch.compile`、Liger 等额外优化；先建立稳定基线，再逐项验证。

Qwen3-VL-8B 的 FP16 权重本身已接近/超过单张 16GB 卡的容量，GRPO 还需要生成、激活、LoRA optimizer state 和 reference 相关状态，因此**不能依赖单卡完整模型聚合**。实际显存由视觉 token、生成长度、group size 和框架实现共同决定，不能仅凭参数量估算“肯定能跑”。

### 3.3 MS-SWIFT 的主要风险

1. **当前 vLLM 路径不可作为 V100 基线**。MS-SWIFT 的 Qwen3-VL 最佳实践要求 vLLM 0.11 或更高；vLLM 官方 V0 弃用 RFC 明确将 V100 支持列为永久移除项，且 v0.11 起移除 V0。旧版 vLLM 曾支持 compute capability 7.0，但早于 Qwen3-VL 的当前受支持路径。因而截至调研日，**没有找到同时覆盖 Qwen3-VL 与 V100 的官方受支持 vLLM 版本组合**。这不等价于证明任何非官方补丁都绝对无法运行，但本项目不能把它作为稳定方案。
2. **Transformers rollout 较慢且可能更吃训练显存**。需要通过已确认的最小 generation batch、ZeRO-3 generation 策略和必要时 CPU parameter offload 换取稳定性。
3. **FP16 数值风险**。MS-SWIFT 文档虽列 V100 和 FP16，但也提示部分模型在 T4/V100 上可能出现 NaN。需要记录 loss scale、梯度范数和异常 completion 比例。
4. **动态 GIF 语义未被直接保证**。官方数据 schema 支持视频/帧列表，但没有证据保证直接读取 `.gif` 会保留全部动画帧，因此必须预解帧。

## 4. 备选：原生 Hugging Face 栈

建议组合：Transformers + TRL + PEFT + Accelerate/DeepSpeed，不额外引入自建训练框架。

### 4.1 能力覆盖

TRL 官方示例目录包含 Qwen-VL SFT notebook 和 Qwen3-VL GRPO notebook；`SFTTrainer` 与 `GRPOTrainer` 支持 VLM 的 `image`/`images` 字段、PEFT 配置和自定义 Python reward。`GRPOConfig.beta>0` 启用 KL/reference；`use_vllm=false` 时可用 `model.generate`；DeepSpeed ZeRO-3 和 `ds3_gather_for_generation=false` 可用于减轻单卡完整模型聚合压力。

因此它在功能上不是“做不到”，而是需要项目自行确保：

- SFT 与 GRPO 使用完全相同的 processor、chat template 和图像 token 策略；
- SFT adapter 以可训练方式加载，同时正确创建/切换 reference adapter；
- 冻结 ViT、merger/projector、embedding、`lm_head` 后，只剩预期 LoRA 参数可训练；
- GIF 解帧、帧序列传入和 batch collator 都有项目级测试；
- reward 的 JSON 解析、错误分数、R5 调用、缓存与日志格式由项目自行实现；
- DeepSpeed、PEFT、TRL、Transformers 各版本之间的 adapter 和 ZeRO-3 行为经过联合测试。

### 4.2 与 MS-SWIFT 的直接比较

| 维度 | MS-SWIFT | 原生 HF 栈 |
|---|---|---|
| Qwen3-VL 模板/数据 | 框架内置，图像/多图/视频/帧列表文档较完整 | Transformers 有 processor，训练侧 collator 与模板一致性需自行维护 |
| SFT→GRPO | 有明确 `adapters`/`ref_adapters` 参数契约 | 可实现，但 adapter/reference 装载逻辑由项目负责 |
| R5 reward | plugin 接口，能读取 completion 与数据集附加列 | 任意 Python reward，灵活性最高，但工程代码更多 |
| 分布式 | 封装 DeepSpeed/FSDP 参数，官方多模态 GRPO 示例较近 | Accelerate/DeepSpeed 能力完整，配置与排错成本较高 |
| 版本面 | 仍需锁版本，但由单一框架给出推荐兼容范围 | TRL/PEFT/Transformers/Accelerate/DeepSpeed 五方兼容面更大 |
| 可定制性 | 高，受框架契约约束 | 最高 |
| 本项目推荐 | **主选** | **MS-SWIFT 遇到阻断性 bug 时的备选** |

若 smoke test 发现 MS-SWIFT 在 reference adapter、ZeRO-3 generation 或 Qwen3-VL processor 上存在无法绕过的问题，再切换原生 HF 栈；不应一开始就为“理论上的最大可定制性”承担额外复杂度。

## 5. 其余框架为何不选

### 5.1 veRL

veRL 是成熟的分布式 RL 框架，官方有多模态 GRPO、KL、自定义 reward，以及 vLLM/SGLang/FSDP 等能力，近期发布记录也包含 Qwen3-VL/LoRA 相关工作。问题在于它的优势主要来自 rollout/training 解耦和高吞吐推理引擎，而本项目当前是单机 8×V100、约 1 万样本，且不能采用当前 Qwen3-VL 的受支持 vLLM 路径。

官方虽已公布 Transformers/HF rollout 方向，但没有找到“Qwen3-VL-8B + V100 + LoRA GRPO”的完整官方案例。选择 veRL 会同时引入 Ray/资源编排、actor/ref/rollout 多组件配置和额外兼容面，却无法确定获得其核心吞吐收益。因此列为未来硬件升级后的候选，而非本项目当前主选。

### 5.2 OpenRLHF

OpenRLHF 支持 GRPO、自定义 reward 和 VLM 方向，但当前架构主要围绕 Ray + vLLM + DeepSpeed。官方仓库截至调研日仍有开放 issue 询问 Qwen3-VL GRPO 支持；现有文档提到的新版 VLM 模型不等于已经证明本项目的 Qwen3-VL 路径。再叠加 V100/vLLM 版本冲突，未通过硬门槛。

### 5.3 LLaMA-Factory

LLaMA-Factory 明确支持 Qwen3-VL 多模态 LoRA SFT、视频数据和 DeepSpeed/FSDP，适合作为 SFT 工具。但官方主仓库没有原生 GRPO trainer/reward API；README 对多模态 GRPO 指向独立项目。若 SFT 用 LLaMA-Factory、GRPO 再换框架，会增加 adapter 名称、模板、processor、tokenizer 和 checkpoint 语义不一致的风险。由于 MS-SWIFT 能统一两阶段，没有必要主动拆栈。

### 5.4 Qwen 官方 qwen-vl-finetune

这是理解 Qwen3-VL 数据处理与 SFT 的权威参考，也支持 LoRA、视觉/MLP/LLM 冻结和 DeepSpeed。但它不包含 GRPO、KL、rollout 或自定义 reward；官方 requirements/示例还偏向 FlashAttention-2 与 BF16。V100 不支持官方 FlashAttention-2 CUDA 后端，必须改 attention backend 和 FP16，且没有官方 V100 训练案例。因此只能作为参考实现，不能作为本项目端到端训练框架。

另一个需留意的实现细节是：当前官方训练代码使用类似 `lora_dropout = training_args.lora_dropout or 0.05` 的写法，显式传入 `0.0` 会回退到 `0.05`。如果未来复用该代码，必须测试最终生成的 PEFT 配置，而不能只看命令行参数。

## 6. 版本与兼容性风险

### 6.1 FlashAttention-2

FlashAttention 官方仓库说明，CUDA 版 FlashAttention-2 支持 Ampere、Ada、Hopper；V100 属于 Volta，不在支持列表。因此：

- 不安装、不启用 FlashAttention-2 作为本项目基线；
- Qwen3-VL 文档中的 FlashAttention-2 是性能建议，不是功能必需条件；
- 先用 PyTorch SDPA，失败时退回 eager；两者都要做同一小批数据的输出与显存对比。

### 6.2 vLLM

不能简单说“vLLM 历来不支持 V100”：旧版官方文档确实曾支持 compute capability 7.0。准确结论是：

- 旧版 vLLM（例如 0.6 系列）曾支持 V100；
- **当前 stable vLLM 官方安装文档要求 NVIDIA GPU compute capability ≥7.5，而 NVIDIA 官方表中 V100 为 7.0，因此 V100 不在当前 stable 的支持范围内**；
- vLLM 官方 V0 弃用计划将 V100 支持列为永久移除，0.11 起移除 V0；
- MS-SWIFT 的 Qwen3-VL 最佳实践要求 vLLM 0.11 或更高；
- 因此截至 2026-08-05，**未找到 Qwen3-VL 当前受支持版本与 V100 的官方兼容交集**。

项目首版必须明确设置 `use_vllm=false`，避免依赖默认值在升级后变化。也不建议为了 V100 固定到旧 vLLM，因为那会进入未经官方证明的 Qwen3-VL 组合。

### 6.3 LMDeploy 的适用边界

LMDeploy 安装文档明确支持 Volta SM70/V100，其模型表支持 Qwen3-VL 的 PyTorchEngine；MS-SWIFT 的独立 infer/deploy 也支持 `infer_backend=lmdeploy`。因此本项目使用同一批真实样本比较：

- Transformers：两卡 `balanced_low_0`/非对称 `max_memory`，必要时完整移动 vision tower 并保持语言层连续分段；
- LMDeploy PyTorchEngine：两卡 TP=2、`lmdeploy_vision_batch_size=1`。

比较逐卡峰值显存、OOM率、吞吐、四字段 JSON 有效率和输出一致性后，再决定数据制作、评估或部署的独立推理后端。该对照不改变 GRPO：当前 MS-SWIFT rollout 不能直接使用 LMDeploy；若未来采用“6卡 trainer + 2卡 LMDeploy”，需要自建请求、policy/LoRA 权重更新与同步，并且 `num_generations=4` 时最小合法 `generation_batch_size` 将从8变为12。

### 6.4 依赖锁定

不要只写宽泛版本范围或直接安装 main/latest。建议在 smoke test 通过后同时冻结：

- MS-SWIFT 的 release tag 或 commit；
- Python、PyTorch、CUDA runtime、Transformers、PEFT、TRL、Accelerate、DeepSpeed、qwen-vl-utils 的精确版本；
- Docker/容器镜像 digest 与所有 wheel 文件哈希；
- Qwen3-VL-8B-Instruct 的 Hub revision 和模型文件哈希；
- processor/chat template、数据集 manifest、GIF 解帧脚本、R5 规则版本、reward plugin commit；
- 随机种子、DeepSpeed JSON 和启动命令。

MS-SWIFT 当前文档给出的依赖范围会随主分支变化，Qwen3-VL 又要求较新的 Transformers/qwen-vl-utils。应以“通过 V100 smoke test 的组合”为唯一基准，再导出 lockfile/`pip freeze`，而不是在需求文档里永久写死尚未验证的最新版。

## 7. 建议的最终框架边界

建议将责任划分固定为：

- **MS-SWIFT**：数据模板、SFT、GRPO、LoRA adapter、KL reference、DeepSpeed ZeRO-3、checkpoint。
- **项目预处理层**：动态 GIF 确定性解帧、图像/帧 token 上限、数据 schema 校验。
- **项目 reward plugin**：严格 JSON 解析；校验 `risk_type`、`evidence`、`description`、`cot`；调用 R5；返回可分解奖励和诊断信息。
- **R5**：只根据 `risk_type + evidence` 决定规则奖励/二元结果；VLM 不直接学习固定的“10 类→二元”映射。

框架不解释项目 `cot` 为模型原生 thinking。Qwen3-VL-8B-Instruct 的原生 thinking 在 SFT、GRPO、评估和部署中保持关闭，输出只允许四字段 JSON；LoRA rank/dropout 已由需求锁定。普通学习率、epoch、scheduler 等由实现侧依据 smoke test 和 dev 曲线自行决定。

## 8. 开训前 smoke test 清单

只有以下项目全部通过，才算 MS-SWIFT 在本项目中“确认可用”：

1. **环境检查**
   - 8 张 V100 均被识别为 compute capability 7.0；记录驱动、CUDA、PyTorch 和全部 Python 依赖版本。
   - 明确验证未加载 FlashAttention-2、未启动 vLLM。

2. **多模态数据检查**
   - 至少各取一条单图、动态 GIF 样本。
   - GIF 用固定采样规则解帧并作为一个 video 帧列表输入，确认帧顺序、数量、分辨率与重复运行完全一致。
   - 验证静态图1024 token、GIF 256 token/帧的动态分辨率上限生效；processor 产生合法 prompt，超长样本被隔离而不是截断。

3. **最小 SFT**
   - 8～16 条样本，8 卡 ZeRO-3，micro-batch=1，至少完成 2 个 optimizer step。
   - 打印并断言：只有预期语言层 LoRA 参数 `requires_grad=true`；ViT、merger/projector、embedding、`lm_head` 无梯度。
   - 无 OOM、NaN/Inf；记录各卡峰值显存。

4. **adapter 保存与重载**
   - 保存 SFT adapter，在新进程重载；同一固定输入的 logits/生成结果在允许误差内一致。

5. **最小 GRPO**
   - 从 SFT adapter 启动；`beta>0`；`use_vllm=false`；`ds3_gather_for_generation=false`。
   - `num_generations=4`、`generation_batch_size=8`，即2个 prompt ×4个 completion，至少完成2次更新。
   - reward plugin 能区分：合法 JSON、缺字段、非法 `risk_type`、无效 evidence、R5 正负反馈。
   - 确认 reference 路径没有在单卡意外复制完整 8B 模型；确认各卡峰值显存保留至少约 1GB 安全余量。

6. **恢复与可复现性**
   - 从 GRPO checkpoint 恢复一次，确认 global step、optimizer/scheduler、adapter 和 reference 指向正确。
   - 固定 seed 的短跑结果可重复；记录无法完全确定性的算子。

7. **OOM 降级顺序**
   - 依次检查：视觉上限是否生效与异常样本 → prompt/completion 分布 → ZeRO bucket/prefetch → `num_generations`/batch；不静默减少 GIF 帧数。
   - gradient checkpointing 默认已启用；纯 GPU 仍 OOM 时才尝试 CPU parameter offload，首版不使用 NVMe offload。
   - 上述仍不能稳定运行时，才切换 QLoRA；切换后重新执行全部 smoke test。

如果 MS-SWIFT 在第 4～6 项出现无法修复的 adapter/reference 或 ZeRO-3 generation 问题，再启动原生 HF 栈的同构 smoke test，而不是并行维护两套正式训练代码。

## 9. 官方来源

以下均为官方仓库或官方文档，查阅日期均为 **2026-08-05**；滚动文档可能变化，正式实施时应把实际采用页面对应的 commit 一并归档。

### MS-SWIFT

- [MS-SWIFT README：Qwen3-VL、多模态、LoRA、GRPO、分布式能力](https://github.com/modelscope/ms-swift/blob/main/README.md)
- [Qwen3-VL Best Practice](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/BestPractices/Qwen3-VL-Best-Practice.md)
- [Multimodal GRPO Best Practice](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/BestPractices/GRPO-Multi-Modal-Training.md)
- [GRPO Get Started：KL、rollout、vLLM/Transformers 路径](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/GRPO/GetStarted/GRPO.md)
- [GRPO 自定义 reward 开发文档](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/GRPO/DeveloperGuide/reward_function.md)
- [命令行参数：freeze、adapter、DeepSpeed、GRPO 参数](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/Command-line-parameters.md)
- [自定义数据集：images、videos、视频帧列表](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Customization/Custom-dataset.md)
- [Transformers rollout 官方示例](https://github.com/modelscope/ms-swift/blob/main/examples/train/grpo/internal/transformers.sh)

### Hugging Face

- [TRL 官方示例目录：Qwen-VL SFT 与 Qwen3-VL GRPO](https://huggingface.co/docs/trl/example_overview)
- [TRL Qwen3-VL GRPO notebook](https://github.com/huggingface/trl/blob/main/examples/notebooks/grpo_qwen3_vl.ipynb)
- [TRL SFTTrainer：VLM 数据与 PEFT](https://huggingface.co/docs/trl/sft_trainer)
- [TRL GRPOTrainer：VLM、custom reward、KL、generation/ZeRO-3](https://huggingface.co/docs/trl/grpo_trainer)
- [Transformers Qwen3-VL 模型文档](https://huggingface.co/docs/transformers/model_doc/qwen3_vl)
- [PEFT 与 Transformers 集成](https://huggingface.co/docs/transformers/peft)
- [Transformers DeepSpeed 文档](https://huggingface.co/docs/transformers/deepspeed)

### 其他候选与硬件兼容

- [veRL 多模态训练示例](https://verl.readthedocs.io/en/latest/examples/multi_modal_example.html)
- [veRL GRPO 文档](https://verl.readthedocs.io/en/latest/algo/grpo.html)
- [OpenRLHF 官方仓库](https://github.com/OpenRLHF/OpenRLHF)
- [OpenRLHF：Qwen3-VL GRPO 支持开放 issue #1175](https://github.com/OpenRLHF/OpenRLHF/issues/1175)
- [LLaMA-Factory 官方仓库](https://github.com/hiyouga/LLaMA-Factory)
- [LLaMA-Factory Qwen3-VL LoRA SFT 示例](https://github.com/hiyouga/LLaMA-Factory/blob/main/examples/train_lora/qwen3vl_lora_sft.yaml)
- [Qwen3-VL 官方 qwen-vl-finetune](https://github.com/QwenLM/Qwen3-VL/tree/main/qwen-vl-finetune)
- [Qwen3-VL 官方仓库](https://github.com/QwenLM/Qwen3-VL)
- [FlashAttention 官方 CUDA 支持范围](https://github.com/Dao-AILab/flash-attention#nvidia-cuda-support)
- [vLLM stable GPU 安装要求：compute capability ≥7.5](https://docs.vllm.ai/en/stable/getting_started/installation/gpu/)
- [NVIDIA Legacy CUDA GPU 表：V100 compute capability 7.0](https://developer.nvidia.com/cuda/gpus/legacy)
- [vLLM 官方 V0 弃用 RFC：V100 支持移除计划](https://github.com/vllm-project/vllm/issues/18571)
- [vLLM 0.6.1 历史安装文档：当时支持 compute capability 7.0](https://docs.vllm.ai/en/v0.6.1/getting_started/installation.html)
- [LMDeploy 安装与 Volta/V100 支持](https://lmdeploy.readthedocs.io/en/stable/get_started/installation.html)
- [LMDeploy 支持模型：Qwen3-VL PyTorchEngine](https://lmdeploy.readthedocs.io/en/stable/supported_models/supported_models.html)
- [LMDeploy 在线权重更新接口](https://lmdeploy.readthedocs.io/en/latest/advance/update_weights.html)
