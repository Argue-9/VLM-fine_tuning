# Qwen3-VL 内容审核微调 — 开发计划时间线

- 训练资源：8× NVIDIA Tesla V100-SXM2-16GB
- 微调模型：Qwen3-VL-8B-Instruct
- 微调方法：Lora + SFT + GRPO
- 数据集输出格式与字段：

```
{
  "description": "客观内容描述",
  "evidence": ["证据枚举"],
  "risk_type": "内容细类",
  "cot": ["证据与类别确认说明"]
}
```

---

## 1.数据搜集（预期2天）

**需要尽快确定risk_type和evidence。另外，能精简就精简，当前risk_type种类40+，每个种类都要覆盖对数据搜集难度增加。**

1.原始数据：图片（png、gif）+ `human_label `

2.数据数量与分布：

- 总数量为1w左右，大体上 good : risk = 6 : 4
- 混淆矩阵分布（通过vlm_advice和human_label是否一致计算）

  |           | ref_good | ref_risk |
  | --------- | -------- | -------- |
  | pred_good | 3k       | 2k       |
  | pred_risk | 3k       | 2k       |

  > 困难正样本（good判为risk）容易搜集，困难正样本（risk判为good）还要考虑每类risk_type覆盖，搜集较难。暂定每类至少10张困难正样本。

- **根据混淆矩阵结果定向再捞数据，直到大致满足**
- 难题：如果考虑数据来源（OBS、DNS）更复杂，但是好像每个来源的图片类型大致确定。例如（只是举例不一定正确）：DNS来源的数据一般都是porn。 因此，捞数据的时候，尽可能覆盖每个数据源即可，先不考虑实际的来源数量门槛。

交付：

- 初步的数据集（图片、csv表格信息）
- 数量与分布计算脚本（good : risk、混淆矩阵）

## 2.数据构造（预期3天）

1.Prompt构造：

面向human_label构造对应的prompt，包含对应的risk_type和evidence

2.模型推理：

- 本地部署模型推理：Qwen3-VL-32B-Instruct
- 推理引擎：LMDepoly PytorchEngine (V100支持，加快推理速度)
- 输出：description、evidence、risk_type、cot

交付：

- 推理脚本
- 初步训练数据集

## 3.人工审核（预期2天）

1.初步数据处理：

得到的risk_type和evidence经过规则引擎输出vlm_advice。这一步先标注，待人工一并审核

2.可视化看板审核：

审核内容包括：human_label、description、evidence、risk_type、cot、vlm_advice

功能：可以直接看图片和上面提及的字段，并且支持直接修改（例如：人工给图片选择human_label、evidence、risk_type）

> 功能细节待确认：核心是为了方便审图

3.结果分析

- 如果是human_label错误，需要纠正并重新推理制作cot
- 如果是human_label正确，推理的过程错误，可人工直接纠正

交付：

- risk_type、evidence→vlm_advice规则脚本
- 审核后的数据集
- 审核看板

## 4.微调训练（预期7天）

1.数据集格式处理

转换为jsonl适用于训练格式

2.冒烟测试先确认显存相关参数

3.正式训练

4.结果分析、定向补充数据

交付：

- jsonl数据集
- 训练代码
- 结果分析脚本
