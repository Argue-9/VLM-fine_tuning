# 首版训练全程冻结视觉编码器

`Qwen3-VL-8B-Instruct` 的 SFT 与 GRPO 首版训练都冻结视觉编码器，只适配多模态输出侧。当前约1万样本主要用于学习内容细类、证据边界、CoT 和 JSON 契约，不足以安全重塑基础视觉能力；冻结视觉编码器还能显著降低图片与 GIF 反向传播的激活显存，并减少业务奖励直接扭曲视觉表征的风险。只有错误切片证明存在稳定的视觉感知缺口时，才另行开展视觉 LoRA 实验。
