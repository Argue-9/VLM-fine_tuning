# 隔离 R5 与业务真值冲突的样本

SFT 和 GRPO 主训练集只接收参考 `risk_type/evidence` 经 R5 映射后与 `human_label` 一致的样本。未解决的冲突样本进入 `r5_conflict` 隔离集，因为同时用真实中间标签进行 SFT、又按最终业务真值进行 RL，会诱导模型隐瞒或篡改 evidence 以适配 R5 缺陷；冲突必须通过修正业务真值、修正中间标注或修复 R5 解决，而不能由模型输出失真来掩盖。
