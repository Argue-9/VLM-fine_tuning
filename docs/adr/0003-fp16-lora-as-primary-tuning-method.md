# 使用 FP16 LoRA 作为主微调方法

`Qwen3-VL-8B-Instruct` 的 SFT 和 GRPO 都使用 FP16 LoRA，并通过参数分片、梯度检查点及视觉 token 控制满足 8×V100-16GB 约束。全参数微调因模型、梯度和优化器状态缺少安全的激活空间而排除；4-bit QLoRA 虽节省底座显存，但会引入量化误差、反量化开销和额外兼容风险，因此只在 FP16 LoRA 经显存收缩后仍 OOM 时作为备用。
