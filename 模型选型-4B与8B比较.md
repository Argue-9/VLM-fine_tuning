# Qwen3-VL 4B 与 8B 选型比较

## 当前结论

- **已选择 Qwen3-VL 8B。** 8B 是能力优先方案；已知原生效果优于4B，视觉编码器和文本表征更强，更适合 OCR、困难负样本、多风险内容及相邻细类边界判断。
- **Qwen3-VL 4B 是已评估但未采用的资源优先方案。** 它的显存和计算余量更大，可支持更高图像分辨率、更多 GIF 帧或更多 GRPO 候选，但当前已知效果低于8B。
- 当前没有同条件的4B完整指标；这不再阻塞型号选择，但后续如补做对照实验，应按本文口径记录结果。

## 比较前提

- 硬件：8 × NVIDIA Tesla V100-SXM2-16GB。
- 训练精度：FP16；V100 不提供原生 BF16。
- 任务：图片及 GIF 内容审核。
- VLM 输出：单一 JSON `{description, evidence, risk_type, cot}`。
- `risk_type`：46 类封闭枚举中的单值主类。
- `evidence`：封闭枚举的多值数组；内部顺序不承载语义，首版不专门处理重复项。
- `cot`：1～3 项短步骤，确认 evidence 和 risk_type。
- 训练链：SFT 冷启动后继续 GRPO。
- 显存硬要求：8×V100 上训练稳定且不 OOM。
- 最终已选择 `Qwen3-VL-8B-Instruct`；下文使用 Instruct 版官方配置进行比较。

## 模型结构

| 项目 | Qwen3-VL-4B | Qwen3-VL-8B | 影响 |
|---|---:|---:|---|
| 实际总参数 | 4,437,815,808 | 8,767,123,696 | 8B 约为 4B 的 1.98 倍 |
| 文本层数 | 36 | 36 | 模型深度相同 |
| 文本隐藏维度 | 2560 | 4096 | 8B 的文本表示容量更大 |
| MLP 中间维度 | 9728 | 12288 | 8B 的训练与生成计算量更高 |
| Attention heads | 32 | 32 | 相同 |
| KV heads | 8 | 8 | 两者每 token 的 KV cache 接近 |
| Head dimension | 128 | 128 | 相同 |
| 视觉编码器层数 | 24 | 27 | 8B 的视觉编码器更深 |
| 视觉隐藏维度 | 1024 | 1152 | 8B 的视觉表征能力更强 |

来源：[Qwen3-VL-4B-Instruct 官方配置](https://huggingface.co/Qwen/Qwen3-VL-4B-Instruct/blob/main/config.json)、[Qwen3-VL-8B-Instruct 官方配置](https://huggingface.co/Qwen/Qwen3-VL-8B-Instruct/blob/main/config.json)。

## 静态显存估算

以下数值由实际总参数量计算，不包含激活、CUDA 工作区、通信缓冲、临时 all-gather 缓冲或 KV cache。

| 内存项 | 计算方法 | 4B | 8B |
|---|---|---:|---:|
| FP16 权重 | 2 bytes/参数 | 8.27 GiB | 16.33 GiB |
| 4-bit 原始权重 | 0.5 bytes/参数 | 2.07 GiB | 4.08 GiB |
| 两份 FP16 权重 | 4 bytes/参数 | 16.53 GiB | 32.66 GiB |
| 经典混合精度 Adam 全参状态 | 约16 bytes/参数 | 66.13 GiB | 130.64 GiB |
| 上述全参状态经8卡理想均分 | 总量 ÷ 8 | 8.27 GiB/卡 | 16.33 GiB/卡 |

说明：

- 8B 的一份完整 FP16 权重已超过单张16GB V100的可用显存，不能使用普通 DDP 在每卡复制完整底座。
- 4B 的完整 FP16 权重可装入单卡，但只剩约7.7GiB理论空间；加入视觉激活、LoRA 状态和运行时缓冲后仍然紧张。
- 8B 全参数训练即使进行理想 ZeRO-3 分片，静态模型与优化器状态也几乎占满单卡容量，因此不具备安全的激活空间。
- 两种型号都应优先考虑 PEFT；全参数训练不作为主方案。
- 4-bit 数值只是量化权重的原始位宽，不包含量化常数、分组元数据及运行时反量化缓冲，不能直接视为实际峰值显存。

## LoRA 状态估算

假设冻结视觉编码器，在语言模型每层的 `q_proj`、`k_proj`、`v_proj`、`o_proj`、`gate_proj`、`up_proj`、`down_proj` 上使用 `rank=32`：

| 项目 | 4B | 8B |
|---|---:|---:|
| 约可训练 LoRA 参数 | 62.5M | 87.3M |
| LoRA 参数、梯度及 Adam 状态 | 约0.70～0.93 GiB | 约0.98～1.30 GiB |

该区间取决于是否保留 FP32 master weights、优化器实现以及分片方式。LoRA 自身不是主要显存瓶颈；底座权重、视觉激活和 GRPO rollout 才是主要变量。

## SFT 阶段比较

共同假设：FP16、LoRA、梯度检查点、单卡 micro-batch size 为1，并控制图像 token 数。

| 维度 | 4B | 8B |
|---|---|---|
| 普通 DDP | 有机会在低/中分辨率下运行，但余量有限 | 不可行，完整权重已超过单卡容量 |
| ZeRO-3/FSDP 后的底座权重理想均分 | 约1.03 GiB/卡 | 约2.04 GiB/卡 |
| 工程峰值初步规划区间 | 约6～10 GiB/卡 | 约9～14 GiB/卡 |
| 提高分辨率或 GIF 帧数 | 余量较大 | 更容易触及显存上限 |
| 调大有效 batch | 更容易 | 更多依赖梯度累积 |

峰值区间是用于方案规划的工程估算，不是验收数字。最终必须在固定的图片像素上限、GIF 帧数和输出长度下进行实测。

## GRPO 阶段比较

GRPO 除训练策略模型外，还需要候选生成、参考策略/KL 计算、log-prob 计算和奖励计算。

在 PEFT 路径下，当前 Hugging Face TRL 可以通过临时禁用或切换 adapter 恢复参考策略，而不常驻第二份完整底座；若最终框架不具备这一行为，参考模型显存需要重新计算。来源：[TRL GRPOTrainer](https://github.com/huggingface/trl/blob/main/trl/trainer/grpo_trainer.py)。

4B 与 8B 都有36层、8个 KV heads、128维 head，因此单条序列的 FP16 KV cache 均约为：

```text
2 × 36层 × 8 KV heads × 128维 × 2 bytes
≈ 144 KiB/token
```

因此，8B 的生成 KV cache 不会仅因总参数量接近两倍而翻倍；主要差距来自底座权重、训练激活和计算量。

| 维度 | 4B | 8B |
|---|---|---|
| 短输出、4候选的工程峰值初步规划 | 约10～14 GiB/卡 | 约13～16 GiB+/卡 |
| 增加 `num_generations` | 空间较大 | 很容易 OOM |
| 增加图片分辨率/GIF 帧数 | 更容易 | 需严格限制视觉 token |
| rollout 与训练吞吐 | 较高 | 预计慢约1.6～2倍，需实测 |
| 数值和运行稳定性 | 风险较低 | 更依赖分片、量化和显存调度 |

影响 GRPO 峰值的关键参数包括：

- `num_generations`；
- 单卡 micro-batch size；
- 图片最大像素与 GIF 抽帧数；
- prompt 和 completion 最大长度；
- 是否使用独立生成引擎；
- 生成引擎的显存预留；
- KL 参考策略的实现方式；
- 底座是否量化及其量化方式。

## 任务能力比较

### 8B 的主要优势

- 当前历史实验中，原生8B效果高于原生4B。
- 更强的视觉编码器和文本表示，预期更适合 OCR、小字、复杂页面、多帧 GIF 和多风险内容。
- 更有能力区分 `porn_suggestive`、`good_risk_signal`、`login_*`、`fraud_single_weak` 等误报敏感边界。
- 更容易维持 `description → evidence → risk_type → cot` 的跨字段一致性。
- 对稀有 risk_type 和困难负样本通常具有更高的能力上限。

### 8B 的主要代价

- 底座权重接近4B的两倍。
- 必须使用参数分片或量化，配置复杂度更高。
- SFT 和 rollout 吞吐更低，完整实验周期更长。
- GRPO 可用候选数、图片分辨率和 GIF 帧数更受限制。
- 单卡仅16GB时显存余量较小，OOM 和 FP16 数值稳定性调试成本更高。

### 4B 的主要优势

- 显存、吞吐和实验迭代明显更友好。
- 可以把节省的资源用于更多 GRPO 候选、更高输入分辨率或更多 GIF 帧。
- 本任务采用46类封闭枚举和短 JSON 输出，任务空间相对受限，4B可能通过高质量 SFT 获得足够能力。
- 训练配置更保守，满足“稳定、不 OOM”硬门槛的概率更高。

### 4B 的主要风险

- 原生效果已观察到略低于8B，但当前缺少可量化差距。
- 可能更容易遗漏细小文字和弱视觉信号。
- 对相邻类别边界和多风险主类选择的能力可能不足。
- RL 可能改善格式和已知边界，但不能保证补回底座缺失的视觉识别能力。

## 后续对照与训练实测

### 可选的同条件质量比较

如后续补做4B对照，应在完全相同的2214样本、提示词、图像预处理、生成参数和 R5 版本下获得：

- 4B 与8B的二元 risk precision/recall；
- 10类业务分类混淆矩阵；
- 逐样本预测，用于比较错误重叠；
- 重点切片：OCR、小字、GIF、困难负样本、`login_*`、`good_risk_signal`、`porn_suggestive`、诈骗弱信号及稀有类；
- JSON 合法率、枚举合法率和跨字段一致率。

### 8B 训练实测

8B 需要分别运行短 SFT 和短 GRPO smoke test，记录：

- 每卡峰值显存；
- tokens/s 或 samples/s；
- 单步耗时；
- `num_generations` 可达到的稳定上限；
- 支持的图片像素和 GIF 帧数上限；
- 是否发生 OOM、NaN、Inf 或梯度溢出；
- 端到端预计训练时长。

## 选择依据

- 当前已知4B原生效果低于8B，而任务包含 OCR、困难负样本、多风险判断和稀有细类，因此优先保留底座能力。
- 8B 增加的显存与训练复杂度通过 PEFT、参数分片、梯度检查点和受控视觉 token 解决，不通过降低模型规模规避。
- “训练稳定、不 OOM”仍是8B方案的硬约束；若 smoke test 失败，应先调整训练配置并记录资源瓶颈，而不是无记录地切换型号。

最终定位：

```text
8B = 已选能力优先方案
4B = 未采用的资源优先替代方案
```
