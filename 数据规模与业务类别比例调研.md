# 数据规模与业务类别比例调研

> 研究对象：Qwen3-VL-8B-Instruct，FP16 LoRA，SFT → GRPO；约 10,000 条业务图片/GIF；46 个 `risk_type`、10 类 `human_label`；训练集/开发集为 90%/10%；首版无类别加权 sampler，类别覆盖通过数据收集实现。
>
> 本文中的数量均指完成四字段参考标注、通过 schema/taxonomy/R5 一致性门禁、可进入主训练集的**有效样本**，不是原始抓取量或尚未确认的候选量。

## 1. 结论先行

10,000 条高质量业务样本**足以作为本项目第一版 SFT → GRPO 的启动规模**，但不能仅凭总量宣称“已经足够覆盖 46 类”或“继续收集不会提升”。

更准确的判断是：

- 对固定四字段 JSON、常见 `good`、porn、gamble 以及已有较强底座能力，10k 很可能足以形成可用的第一版 actor；
- 对历史样本中接近零覆盖的 finance、fake、other_fraud、politic、vpn、game、login，以及这些域下的细粒度 `risk_type`，10k 只能提供最低可学习覆盖，不足以保证稳定的逐类泛化指标；
- 10k 是否最终足够，必须由 2.5k / 5k / 7.5k / 10k 的嵌套学习曲线决定；
- 数据质量、视觉/evidence 多样性和边界样本，比把大量相似模板堆到 10k 更重要；
- `num_generations=4` 只增加同一 prompt 的 rollout 数，不会把 60 张不同图片变成 240 张独立语义样本。

推荐的首版 10k 构成为：

| 大桶 | 数量 | 占比 |
|---|---:|---:|
| `human_label=good` | 6,500 | 65% |
| 其中 `good_clean` | 4,500 | 45% |
| 其中 `good_risk_signal` | 2,000 | 20% |
| 其余 9 类 `human_label` | 3,500 | 35% |
| 合计 | 10,000 | 100% |

对 44 个非-good `ref_risk_type`，建议每类**至少 60 条有效且有内部多样性的样本**：

\[
44\times60=2640
\]

剩余 860 条非-good 预算再按历史频率、历史错误、业务严重性和边界复杂度定向补充。两个 good `risk_type` 分别已有 4,500 和 2,000 条，因此 46 类的算术覆盖闭合。

项目最终固定2,000条 `good_risk_signal` 作为困难/边界硬配额，用于压低 good 被误报为其余业务类别的概率。考虑到困难非-good样本难以稳定找到且人工成本较高，首版不再规定500条总量或逐 `risk_type` 配额；采集中自然遇到则保留和统计，训练诊断证明必要后再定向补充。

这组 65/35 与 60/20/20 等比例是**首轮工程起点，不是普适最优比例**。若学习曲线显示尾类仍明显受数据限制，可转向 60/40；若真实先验评估显示误报恶化，则应向 70/30 回调或优先增加困难 good。

## 2. 必须先区分的三个问题

### 2.1 总量是否足够

“10k 是否足够”讨论的是整个 actor 能否学习：

- canonical actor prompt；
- 四字段 JSON 和字段类型；
- `description`、`evidence`、`risk_type`、`cot` 的关系；
- 46 类 taxonomy；
- 图片与最多 4 帧 GIF 的视觉任务；
- 经 R5 后的业务目标。

总量可以足够教会输出协议，但不能保证每个尾类都足够。

### 2.2 每类有效样本是否足够

46 类不是一个均匀问题。即使 10k 平均约为 217 条/类，推荐配比下也不会均匀：good 类需要维护真实业务先验和误报边界，而 44 个非-good 类只能获得约 60–数百条。

对某个 `risk_type`，真正重要的是：

```text
有效样本数
× 标签正确率
× 视觉模板多样性
× evidence 组合多样性
× 媒体形态覆盖
```

60 张来自 60 个真实变化场景，通常比 300 张同一页面模板更有信息量。本文的“每类 60”是采集下限，不是统计充分性证明。

### 2.3 开发集是否足够稳定

10k 按 90/10 划分后只有约 1,000 条 dev。若某个尾类总计 60 条，分层后大约是：

```text
train ≈ 54
dev ≈ 6
```

在最不利的正确率 50% 附近，二项比例标准误差上界约为：

\[
SE_{max}=\frac{0.5}{\sqrt n}
\]

当 `n=6` 时，标准误差约 20.4 个百分点，95% 区间更宽。因此：

- 1k dev 可以稳定观察总体、good/non-good、头部 `human_label` 和格式指标；
- 不能把只有 6–10 条样本的单个 `risk_type` 准确率当成稳定结论；
- 尾类 dev 只用于发现明显失败、零预测或 schema 问题；
- 逐 `risk_type` 的可靠统计结论需要更多独立诊断样本，不能由当前每类约 6 条的 dev 切片支持。

## 3. 2,214 条历史回归数据给出的信息

### 3.1 历史类别先验

历史 2,214 条的真实 support 为：

| 类别 | support | 占比 |
|---|---:|---:|
| good | 1,559 | 70.42% |
| porn | 405 | 18.29% |
| gamble | 223 | 10.07% |
| finance | 16 | 0.72% |
| other_fraud | 5 | 0.23% |
| fake | 3 | 0.14% |
| game | 3 | 0.14% |
| politic | 0 | 0% |
| vpn | 0 | 0% |
| login | 0 | 0% |

历史 `good` 占比的点估计为：

\[
1559/2214=70.42\%
\]

若把这批数据近似视为独立同分布样本，其 Wilson 95% 区间约为 68.48%–72.28%。但这批数据是历史回归集，并未确认是从当前线上流量随机抽样，因此不能把该区间误当成未来生产先验。

### 3.2 机械按历史比例放大为什么不可行

若直接把历史 support 放大到 10k：

| 桶 | 约计样本数 |
|---|---:|
| good | 7,041 |
| porn | 1,829 |
| gamble | 1,007 |
| 其他全部业务类合计 | 123 |

后 123 条要覆盖 finance、fake、other_fraud、politic、vpn、game、login 及其大量细粒度 `risk_type`，显然不可能。

因此，历史数据可以用于估计“good 大约占多数、porn/gamble 是头部”，不能直接作为 46 类训练采样表。

### 3.3 历史错误说明需要同时补 good 边界和尾类正例

原生 8B + R4 历史基线中：

- 1,559 条 good 只有 1,296 条被正确识别，263 条被预测成其他类；
- 二元口径的 risk false positive 正是这 263 条，说明误报是当前 precision 的主要问题；
- 655 条非-good 中约 86 条被预测为 good，说明召回也不能只靠增加 good；
- gamble 的 precision 60.59%、recall 73.09%，是明确的头部改进对象；
- finance 只有 16 条真实样本却出现 123 个 FP，说明它既缺正例，也缺与 payment/mall/普通业务页面相邻的困难 good；
- game、fake、other_fraud 支持极少，politic、vpn、login 在历史集中没有可用 support。

按历史矩阵可重建二元结果：

```text
实际非-good = 655
正确识别非-good ≈ 569
good 被报为非-good = 263
非-good 被报为 good = 86
```

对应 risk precision 约 68.4%、risk recall 约 86.9%，与任务说明一致。

这解释了推荐配比为什么不是简单做成 50/50：本项目要提高召回，但同时必须用大量真实 good 和风险相似 good 控制误报。

## 4. 一手研究能证明什么，不能证明什么

### 4.1 少量高质量数据可以教会大模型新输出行为

[LIMA](https://arxiv.org/abs/2305.11206)用 1,000 条精心挑选的文本 prompt/response 对 65B LLaMA 进行 SFT，并展示了少量数据学习回答风格与格式的能力。

这支持“预训练大模型不必从零学习世界知识，10k 可能足以学习任务协议”，但不能证明：

- 1,000 或 10,000 是本项目最优规模；
- 文本 65B 的结论等同于多模态 8B；
- 少量样本足以覆盖 46 个细粒度视觉类别；
- 自动候选标注中的 taxonomy/R5 错误不会伤害模型。

### 4.2 多模态 SFT 更依赖图像与标注多样性

[MMInstruct](https://arxiv.org/abs/2407.15838)将图像多样性、标注质量和指令多样性列为视觉指令数据的核心问题，并通过跨域消融观察到增加领域覆盖可以改善中英文多模态基准表现。

[Data Diversity Matters for Robust Instruction Tuning](https://arxiv.org/abs/2311.14736)也报告，增加指令数据多样性能够改善最差情形的指令跟随表现，同时强调质量与多样性存在权衡。

这些结果支持：

- 不能只以 raw row count 判断 10k 是否足够；
- 尾类应覆盖不同视觉模板、文字表达、evidence 组合和图片/GIF 形态；
- 质量不合格的“多样性”会变成标签噪声；
- 本项目只有一个 canonical actor prompt，因此应增加媒体和业务语义多样性，而不是人为制造多个问法。

### 4.3 长尾数据会伤害尾类和校准

真实视觉数据常呈长尾。[开放世界长尾识别研究](https://openaccess.thecvf.com/content_CVPR_2019/html/Liu_Large-Scale_Long-Tailed_Recognition_in_an_Open_World_CVPR_2019_paper.html)指出，头类占据大部分数据而尾类只有少量样本时，模型需要同时处理不平衡、少样本和开放集混淆。[长尾校准研究](https://openaccess.thecvf.com/content/CVPR2021/html/Zhong_Improving_Calibration_for_Long-Tailed_Recognition_CVPR_2021_paper.html)进一步表明，类别样本数会影响预测分布与校准。

这些论文不是 VLM 生成式分类实验，但它们说明按 70/30 的总比例并让内部尾类自然掉到个位数，不符合“无加权 sampler、依靠收集解决覆盖”的项目决定。

### 4.4 微调收益随数据量变化，但曲线取决于任务

[When Scaling Meets LLM Finetuning](https://arxiv.org/abs/2402.17193)在数据受限场景研究了 LoRA 等参数高效微调，发现微调性能与数据量存在可拟合的缩放关系，但最优微调方式高度依赖任务和数据。

[Learning Curves for Analysis of Deep Networks](https://proceedings.mlr.press/v139/hoiem21a.html)将测试误差表示为训练样本量的函数，并说明学习曲线可用于分析预训练、架构和数据增强带来的收益。

因此，现有论文不能代替本项目自己的 2.5k/5k/7.5k/10k 曲线，也不存在可直接引用的“8B LoRA + 46 类 VLM 一定需要 N 条”的普适常数。

## 5. 70/30、65/35、60/40 与 50/50 方案比较

这里的 `good/non-good` 是按 `human_label` 和采集桶描述数据构成，不提前假定 46 个 `risk_type` 到最终二元结果的固定映射；最终业务结果仍由 R5 决定。

| 方案 | good | 其余 9 类 | 44 类按每类 60 条后剩余预算 | 优点 | 风险 |
|---|---:|---:|---:|---|---|
| 70/30 | 7,000 | 3,000 | 360 | 最接近历史 70.4% good，较利于控制误报与保留先验 | 尾类只有最低覆盖，几乎没有空间补头部错误和内部多样性 |
| **65/35** | **6,500** | **3,500** | **860** | 保持 good 明显多数，同时为 44 类提供最低覆盖和定向补充 | 仍会使罕见类 dev 很稀疏；比例较历史先验略向非-good 偏移 |
| 60/40 | 6,000 | 4,000 | 1,360 | 尾类和困难正例空间最好，可能改善宏平均召回 | 训练先验明显更偏非-good，可能增加 false positive，偏离真实流量 |
| 50/50 | 5,000 | 5,000 | 2,360 | 非-good 类别拥有最大补充空间，适合作为强调尾类召回的消融对照 | 人为二分不对应 VLM 的 46 类目标，明显偏离 good 居多的业务先验，最需警惕误报上升 |

按 odds 比较，历史样本的非-good/good 约为：

\[
0.296/0.704=0.420
\]

65/35 的训练 odds 为：

\[
0.35/0.65=0.538
\]

约为历史 odds 的 1.28 倍；60/40 则约为 1.59 倍。65/35 是比历史分布温和的尾类富集，不是把类别做成近似均衡。

50/50 的训练 odds 为：

\[
0.50/0.50=1.0
\]

是历史 odds 的约 `1.0/0.420=2.38` 倍。换言之，若线上分布仍接近历史样本，50/50 会把其余 9 类相对于 good 的训练曝光提高到历史相对曝光的约 2.38 倍；这已经不是温和补齐尾类，而是显著改变训练先验。

### 5.1 为什么首版不采用 50/50

第一，50/50 的“平衡”不是本项目真正的类别平衡。VLM 正式输出一个 46 类 `risk_type`，数据还有 10 类 `human_label`；把 `human_label=good` 与其余 9 类各放 50%，只是人为构造两个采集桶。它既不能让 10 类均衡，也不能让 46 类均衡，更不代表 `risk_type` 到二元结果的固定映射。最终二元结果仍由 R5 根据 `risk_type+evidence` 决定。

第二，50/50 会明显削弱真实 good 的覆盖。相对于推荐的 65/35，它会少 1,500 条 good。若仍保持 good 内部约 69% `good_clean`、31% `good_risk_signal` 的结构，则两类都会减少；若坚持保留 2,000 条 `good_risk_signal`，减少的几乎全部是 `good_clean`。这会缩小正常页面、普通内容、不同来源和不同视觉模板的覆盖，不利于模型学习“没有风险证据时不要过度判断”。

第三，历史主要问题之一就是误报。历史 1,559 条 good 中有 263 条被报成其他类别；在二元汇总口径下，这些构成风险侧 false positive。50/50 增加大量其余类别曝光、同时减少 good 曝光，可能让 actor 更倾向输出会被 R5 分派到非-good 结果的 `risk_type/evidence` 组合，从而与“提高 precision、减少误报”的目标冲突。这里不是声称训练比例会机械决定预测比例，而是指出：在首版不使用类别 sampler、类别 reward 倍率或部署先验修正的情况下，没有额外机制抵消这种训练分布偏移。

第四，50/50 对满足最低长尾覆盖并非必要。35% 的其余类别预算已经可以完成：

```text
44 个非-good risk_type × 60 = 2,640
剩余定向补充                =   860
合计                        = 3,500
```

50/50 会把定向补充预算扩大到 2,360 条。只有已经证明这些额外样本具有新的视觉模板、evidence 组合或困难边界价值时，这部分预算才可能有效；否则更容易继续堆积头部或相似模板，不能自动提升 46 类泛化。

第五，训练集与部署流量存在先验偏移时，平衡数据上的 precision 不能直接代表 good 占多数时的 precision。即使 50/50 有助于某些尾类 recall，也必须付出更高的真实流量误报风险，并需要额外的生产先验评估或后处理校准。本项目当前先通过数据收集解决覆盖，尚未确定这类先验修正机制，因此不应把 50/50 作为默认起点。

因此，50/50 不是绝对不能训练，而是不适合作为当前首版主方案。它可以作为后续消融：当 65/35 的学习曲线确认尾类仍显著欠拟合，并且增加至 60/40 后仍未解决，同时真实流量误报没有恶化时，再测试 50/50。当前优先级应是保证 44 类最低覆盖、补充困难样本和提高样本多样性，而不是追求表面上的二桶相等。

推荐先用 65/35，原因是：

1. 当前核心目标不仅是召回，还要改善历史约 68.4% 的 risk precision；
2. 历史上 263 条 good 被误报，good 不能被过度下采样；
3. 纯 70/30 又无法给 44 个非-good risk_type 足够内部多样性；
4. 项目首版没有加权 sampler，数据占比会直接决定训练曝光；
5. 65/35 留出 860 条超过最低覆盖的定向预算，可优先投向 gamble、finance 和零/低支持业务域。

## 6. 推荐的 10k 可执行构成

### 6.1 一级：10 类 `human_label`

| `human_label` 采集桶 | 数量 | 占比 | 推导依据 |
|---|---:|---:|---|
| good | 6,500 | 65.0% | 保留业务多数先验，并控制误报 |
| gamble | 750 | 7.5% | 历史头部且 precision/recall 都需要提升 |
| porn | 700 | 7.0% | 历史最大风险头部；原生已较强，因此不按自然比例放大 |
| finance | 470 | 4.7% | 历史正例极少、FP 极高，需要正例与边界覆盖 |
| politic | 460 | 4.6% | 7 个 risk_type，历史零支持，先满足最低覆盖再略补 |
| other_fraud | 360 | 3.6% | 5 个细类，弱信号边界复杂 |
| login | 320 | 3.2% | 5 个细类，历史无 support，且与普通页面混淆 |
| fake | 260 | 2.6% | 4 个细类，历史仅 3 条 |
| vpn | 90 | 0.9% | 单一 risk_type，历史零支持 |
| game | 90 | 0.9% | 单一 risk_type，历史 3 条且 FP 较多 |
| **合计** | **10,000** | **100%** |  |

非-good 部分的算术为：

```text
8×60 gamble floor     = 480
7×60 porn floor       = 420
6×60 finance floor    = 360
7×60 politic floor    = 420
5×60 other_fraud      = 300
5×60 login            = 300
4×60 fake             = 240
1×60 vpn              =  60
1×60 game             =  60
--------------------------------
44×60                 = 2640
定向补充               =  860
非-good 合计           = 3500
```

表中定向补充后，各业务桶仍须满足“内部每个 risk_type ≥ 60”，不能把一个桶的额外数量全部堆到单一头部小类。

### 6.2 二级：good 内部构成（已确认）

| `ref_risk_type` | 数量 | 占总集 | 占 good |
|---|---:|---:|---:|
| `good_clean` | 4,500 | 45.0% | 69.2% |
| `good_risk_signal` | 2,000 | 20.0% | 30.8% |

`good_risk_signal` 的 2,000 条建议再按“最容易被误报成什么”分层：

| 风险相似方向 | 数量 | 例子 |
|---|---:|---|
| porn 相似但合规 | 450 | 性感但不构成违规、医学/科普、平台标题 |
| finance 相似但合规 | 400 | 合规支付、正常商城、正规投资信息、银行界面 |
| gamble/game 相似但合规 | 300 | 合法彩票资讯、体育比分、普通棋牌游戏 |
| login/客服/二维码/线路相似 | 300 | 普通登录、正常客服、仅二维码、普通网络线路提示 |
| fake/官方/品牌相似但合规 | 300 | 正规品牌页、政府公共服务、反诈宣传 |
| politic/军警/新闻相似但合规 | 150 | 正常新闻、公共安全、合法政府或军警内容 |
| 跨域其他弱信号 | 100 | 多个弱信号但仍不足以构成目标类别 |
| **合计** | **2,000** |  |

这些是采集分层，不改变项目既有 taxonomy；最终 `ref_risk_type/evidence` 仍以标注门禁结果为准。

### 6.3 三级：困难/边界样本（已确认调整）

首版只固定2,000条 `good_risk_signal`，占总集20%，用于覆盖 false positive、precision 和合法相似内容。非-good困难正例不设置500条总量或“每类至少8条”的硬配额，避免数据发现与人工筛选成本阻塞10k主数据集。

采集中自然出现的小字、低清、遮挡、短暂GIF帧、隐晦表达、相邻taxonomy或多evidence并存样本仍应保留，并在不增加人工专项搜索的前提下统计实际数量。若训练后的学习曲线或错误分析显示特定类别明显欠拟合，再对该类定向补充。“困难”不等于“标签不确定”；无法确认reference的样本仍进入冲突/隔离集。

## 7. 每类 60 条应该长什么样

每类最低 60 条不能只满足计数，还应覆盖以下轴：

| 轴 | 需要覆盖的变化 |
|---|---|
| 媒体 | 静态图与业务中真实存在的 GIF；GIF 遵守统一最多 4 帧规则 |
| 文字 | 有/无文字、中文/混合文字、小字、遮挡、低对比度 |
| 视觉主体 | 页面、海报、聊天、真人/物体、局部图标、弹窗/横幅 |
| evidence | 单 evidence、多 evidence、强信号、允许范围内的弱信号组合 |
| 难度 | 明确正例、边界正例、与相邻 risk_type 容易混淆的样本 |
| 表现形式 | 不同品牌、布局、颜色、文案、设备截图和内容来源 |

首版继续只使用现有 `md5` 去重，不引入 pHash、视觉向量或 source 分组；但采集负责人仍应通过配额表防止 60 条被同一模板占满。

如果某个 risk_type 无法收集 60 条真实、多样且标签可靠的样本：

1. 不复制同一图片凑数；
2. 不降低标签门禁；
3. 将该类标记为 `coverage_gap`；
4. 在学习曲线和 dev 报告中单独披露；
5. 结论写成“10k 对该类尚不足”，而不是用总体指标掩盖。

## 8. 数据质量优先级

10k 应指以下门禁后的数量：

```text
原始业务媒体
  → md5 去重与分区隔离
  → 数据制作模型生成 candidate 四字段
  → JSON/schema/闭集枚举检查
  → evidence 可见性与 cot 自洽检查
  → reference 确认
  → R5(ref_risk_type, ref_evidence) 与 human_label 一致
  → 主训练有效样本
```

下列样本不计入 10k 有效规模：

- JSON 可解析但字段语义错误；
- `risk_type`/`evidence` 不在枚举；
- evidence 不可从图片/GIF 观察；
- 为了通过 R5 而删改真实 evidence；
- R5 与 `human_label` 冲突且未解决；
- 同一 `md5` 重复；
- 媒体损坏或 GIF 抽帧不可复现；
- 参考 `cot` 没有按既定规则确认 evidence → risk_type。

如果预期候选通过率为 (q)，原始收集量至少应为：

\[
N_{raw}\ge\frac{10000}{q}
\]

例如通过率 80% 时，原始池至少约需 12,500 条。该公式只是容量规划；真实通过率应由 pilot 统计。

## 9. 真实业务先验与训练富集的权衡

### 9.1 为什么训练集不完全复制线上先验

如果完全复制历史分布，多个 `risk_type` 可能只有 0–几条，模型没有学习机会。由于项目不使用类别 sampler 或类别 reward 倍率，适度定向采集是实现覆盖的唯一首版手段。

### 9.2 为什么训练集也不能过度均衡

precision 与类别先验有关。若以 π 表示真实流量中非-good 先验，给定 TPR 与 FPR：

\[
Precision=
\frac{\pi\cdot TPR}
{\pi\cdot TPR+(1-\pi)\cdot FPR}
\]

在更高非-good 比例的平衡 dev 上测得的 precision，可能高估 good 更多的真实流量表现。训练中过度富集非-good 也可能让 actor 更容易输出非-good 类，从而增加 R5 后误报。

因此：

- 训练用 65/35 做温和富集；
- 1k dev 尽量保持与该训练方案一致，便于 checkpoint 选择；
- 2,214 条历史回归集继续以约 70/30 的固定构成做次级回归，重点看误报和头部连续性；
- 历史回归集不并入训练，也不能反复调参到过拟合；
- 上线前还需要来自真实流量先验的独立评估或按生产先验重加权。

## 10. 2.5k / 5k / 7.5k / 10k 学习曲线

### 10.1 数据构造

先完成并冻结最终 10k 的 90/10 分区：

```text
final_train = 9,000
final_dev   = 1,000
```

从 final_train 构造嵌套、分层子集：

| 名义总规模 | 实际 train 子集 | 固定 dev | train 占最终 9k |
|---:|---:|---:|---:|
| 2,500 | 2,250 | 同一 final_dev 1,000 | 25% |
| 5,000 | 4,500 | 同一 final_dev 1,000 | 50% |
| 7,500 | 6,750 | 同一 final_dev 1,000 | 75% |
| 10,000 | 9,000 | 同一 final_dev 1,000 | 100% |

“名义总规模”表示按 90/10 方案时对应的训练量。为了让曲线可比，四个模型必须都在同一个 1,000 条 final_dev 上评估，而不能各自更换 dev。

嵌套关系必须满足：

\[
D_{2.25k}\subset D_{4.5k}\subset D_{6.75k}\subset D_{9k}
\]

每个子集按 `human_label`、`ref_risk_type`、good 子类、图片/GIF 和困难标记分层。小规模点也要覆盖 46 类；按最终每个非-good 类 60 条计算，2.25k train 子集中每类约有 13–14 条，正好可以观察从极少样本到约 54 条 train 的增长。

### 10.2 训练控制

四个点固定：

- base model、processor 和 prompt/taxonomy 版本；
- LoRA rank/alpha/target/dropout；
- 图像分辨率和 GIF 抽帧；
- optimizer、学习率、scheduler、有效 batch；
- 相同 epoch 数与 checkpoint 选择规则；
- 相同确定性/低温评估配置；
- 至少对关键点使用 2 个 seed，预算不足时至少对 7.5k/10k 复跑。

同 epoch 数使每条样本获得近似相同曝光，计算量会随数据规模增加。这回答的是“继续收集并按同一训练配方使用，能否带来收益”。若还要隔离纯计算量影响，可追加固定 token/step 的次级对照，但不应替代主学习曲线。

### 10.3 每个点都要报告的指标

#### 业务核心

- R5 后 risk recall、risk precision、二元 F1；
- 10 类 `human_label` 的 macro-F1、每类 recall；
- 46 类 `risk_type` exact accuracy、macro recall 与零预测类别数；
- evidence set-F1；
- JSON parse、四键、字段类型、枚举、canonical 顺序通过率；
- `good_risk_signal` 被报成非-good 的比例；
- 若dev中自然存在足够样本，报告困难非-good被报成good的比例；数量过少时只列案例，不形成稳定比率。

#### 分层诊断

- 图片 vs GIF；
- 头部 vs 尾部 risk_type；
- 明确样本 vs 困难/边界样本；
- OCR/页面 vs 自然图像；
- SFT checkpoint vs GRPO checkpoint。

GRPO 的 4 个 completion 只在同 prompt 内做相对优化。报告数据规模时仍按独立媒体 prompt 数计数，不把 rollout 数乘进样本量。

### 10.4 如何判断 10k 是否足够

可用逆幂形式仅作诊断拟合：

\[
E(N)=E_{\infty}+aN^{-b}
\]

四个点不足以可靠外推很远，因此更重要的是直接看相邻增益和置信区间。

可在实验前约定以下工程规则：

- 若 7.5k → 10k 的总体核心指标提升小于 1 个百分点、bootstrap 区间高度重叠、两 seed 方向一致且尾类不再明显上升，可把 10k 视为首版平台期；
- 若总体已经平台，但部分 risk_type 仍有稳定提升或零召回，则只继续定向收集这些类；
- 若 JSON 已平台但 evidence/risk_type 继续提升，说明需要更多语义多样性而不是更多格式重复；
- 若 train 指标持续上升而 dev 下降，优先检查标签噪声、模板重复和过拟合，不机械扩量；
- 若 10k 点仍明显向上，不宣称足够，继续到 12.5k/15k 或按曲线估计下一采集批次。

“1 个百分点”是可预注册的首轮工程阈值，不是论文给出的普适常数。

## 11. SFT 与 GRPO 对数据规模的不同要求

### 11.1 SFT

SFT 负责从底座冷启动正式四字段完整输出，因此最依赖：

- 高质量参考 JSON；
- 每个 risk_type 的最低覆盖；
- evidence 组合和视觉表达多样性；
- good 与非-good 边界；
- 图片/GIF 一致预处理。

若 10k 质量不足，GRPO 不能可靠地从零补回 schema 和标签语义。

### 11.2 GRPO

GRPO 的 group generation 可以增加同一输入上的策略探索，但不会创造新的图片、文字样式或 evidence 组合。尾类只有 54 个 train prompt 时，即使每个 prompt 生成 4 次，其语义覆盖仍是 54 个媒体实例。

因此：

- GRPO 不能作为尾类采集不足的替代方案；
- 由于不使用类别 sampler，GRPO prompt 曝光仍由上述数据构成决定；
- 如果某尾类组内奖励长期零方差，先排查样本是否过少、过易或 reference 有问题；
- 学习曲线应同时比较每个规模的 SFT 与 SFT→GRPO，避免把 SFT 数据收益和 RL 收益混在一起。

## 12. 推荐实施顺序

1. 先收集/制作 2,500 条满足 65/35 和 46 类覆盖的 pilot；
2. 统计 candidate → reference 的实际通过率与各类 `coverage_gap`；
3. 使用相同配比扩展到 5,000 和 7,500；
4. 在每个里程碑保存嵌套 train 子集，不重新随机换一套样本；
5. 10,000 条完成后冻结最终 9k/1k 分区；
6. 用统一 final_dev 重跑四个规模的 SFT 和 SFT→GRPO；
7. 用历史 2,214 条做固定次级回归，不并入训练；
8. 根据曲线决定停在 10k、定向补尾类，还是把 good/non-good 从 65/35 调到 70/30 或 60/40；
9. 任何比例调整都重新看真实先验下的 risk precision，不能只看宏平均 recall。

## 13. 最终建议

本项目当前应把“10k”定义为一个可验证的首版预算，而不是先验充分性结论：

```text
有效总量：10,000
train/dev：9,000 / 1,000

good：6,500
  good_clean：4,500
  good_risk_signal：2,000

其余 9 类 human_label：3,500
  44 个非-good ref_risk_type：每类至少 60
  最低覆盖使用：2,640
  频率/错误/严重性定向补充：860

困难/边界硬配额：2,000
  good_risk_signal：2,000
  非-good困难正例：机会性保留，不设硬配额

充分性验证：
  2.5k → 5k → 7.5k → 10k 嵌套学习曲线
  同一 final_dev
  同时报 SFT 与 SFT→GRPO
```

65/35 的意义是：不照抄历史长尾，也不把训练集做成脱离生产先验的均衡数据。它给 44 个非-good `risk_type` 最低学习机会，同时用 65% good、其中 20% 总量的 `good_risk_signal` 保护 precision。最终比例仍应由学习曲线和真实先验评估修正。

## 14. 主要来源

- Zhou et al., [LIMA: Less Is More for Alignment](https://arxiv.org/abs/2305.11206)
- Liu et al., [MMInstruct: A High-Quality Multi-Modal Instruction Tuning Dataset with Extensive Diversity](https://arxiv.org/abs/2407.15838)
- Bukharin et al., [Data Diversity Matters for Robust Instruction Tuning](https://arxiv.org/abs/2311.14736)
- Chen et al., [When Scaling Meets LLM Finetuning: The Effect of Data, Model and Finetuning Method](https://arxiv.org/abs/2402.17193)
- Hoiem et al., [Learning Curves for Analysis of Deep Networks](https://proceedings.mlr.press/v139/hoiem21a.html)
- Liu et al., [Large-Scale Long-Tailed Recognition in an Open World](https://openaccess.thecvf.com/content_CVPR_2019/html/Liu_Large-Scale_Long-Tailed_Recognition_in_an_Open_World_CVPR_2019_paper.html)
- Zhong et al., [Improving Calibration for Long-Tailed Recognition](https://openaccess.thecvf.com/content/CVPR2021/html/Zhong_Improving_Calibration_for_Long-Tailed_Recognition_CVPR_2021_paper.html)
