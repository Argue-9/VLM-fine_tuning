

# 任务定义

使用 vlm 多模态大模型（Qwen3-VL）对图片（包括 GIF）进行是否违规审核，要求在保证高召回率的前提下提高精确率、减少误报。

当前方案：VLM 输出严格四字段 JSON `{description, evidence, risk_type, cot}`，其中 R5 只消费 46 类 `risk_type` 与封闭枚举 `evidence`（参考 `risk_type.md`），输出 10 类 `vlm_advice` 与 `vlm_confidence`；最终二元 `good/risk` 结果也由 R5 规则决定，不在 VLM 内固定映射。可以将 R5 理解为既有决策树黑盒。对 VLM 做 SFT + RL（GRPO）微调，R5 推理保留，训练侧与其解耦。当前阶段只要求最终得到二元结果与 10 类业务分类，不定义验收门槛。

## 当前识别效果（Qwen3-VL 8B 原生 + R4 历史基线，2214 样本）

| class       |   TP |   FP |   FN | Precision | Recall |     F1 | support |
| ----------- | ---: | ---: | ---: | --------: | -----: | -----: | ------: |
| porn        |  359 |    9 |   46 |    0.9755 | 0.8864 | 0.9288 |     405 |
| good        | 1296 |   86 |  263 |    0.9378 | 0.8313 | 0.8813 |    1559 |
| gamble      |  163 |  106 |   60 |    0.6059 | 0.7309 | 0.6626 |     223 |
| game        |    3 |   29 |    0 |    0.0938 | 1.0000 | 0.1714 |       3 |
| finance     |    5 |  123 |   11 |    0.0391 | 0.3125 | 0.0694 |      16 |
| fake        |    0 |   19 |    3 |    0.0000 | 0.0000 | 0.0000 |       3 |
| other_fraud |    0 |    7 |    5 |    0.0000 | 0.0000 | 0.0000 |       5 |
| politic     |    0 |    2 |    0 |    0.0000 | 0.0000 | 0.0000 |       0 |
| vpn         |    0 |    7 |    0 |    0.0000 | 0.0000 | 0.0000 |       0 |

- macro-P = 0.2947；macro-R = 0.4179；macro-F1 = 0.3015；micro-acc = 0.8248
- 二元 good/risk：risk recall 约 86.9%，risk precision 约 68.4%
- 测过 Qwen3-VL 4B 原生，效果略低于 8B
- 该 2214 样本仅作历史回归集，不作为最终独立盲测集

## 实验设备

8 × NVIDIA Tesla V100-SXM2-16GB

- V100（Volta）无原生 bf16，全程使用 fp16，需监测数值稳定性
- 不依赖临时更大显存或更新架构 GPU；可用 PEFT、量化、梯度检查点、更长训练时间
- 训练稳定、不 OOM 为硬门槛，优先级高于更大模型或更激进输入
- 单机 8 进程、每卡 1 个进程；SFT 与 GRPO 均由 8 张卡共同承载 ZeRO-3 分片 policy，GRPO 不预留两张专用 rollout 卡

## 其他说明（现状）

- 原始数据只有图片/GIF 及相关信息 + `human_label`，无 evidence / description / risk_type，需在数据制作阶段补充。
- `human_label` 是 10 类 advice 业务真值；模型输出是 46 类 risk_type——两者粒度不同。因此 46 类 risk_type 无法直接和 10 类 human_label 比对，需借 R5 做映射。
- 多风险样本由 `human_label` 锚定唯一主风险域，`risk_type` 必须从该域对应的小类中选择；`evidence` 仍须保留所有可观察到的真实信号，不得为了让 R5 命中而删改证据。若 R5 结果与 `human_label` 不一致，标记为规则冲突并交由人工复核。
- 只有将参考 `risk_type/evidence` 输入 R5 后得到的 `vlm_advice` 与 `human_label` 一致，样本才可进入 SFT/GRPO 主训练集。未解决的不一致样本进入 `r5_conflict` 隔离集；不得通过隐瞒或篡改真实 evidence 强行制造一致。

## 目标

- 微调对象：`Qwen3-VL-8B-Instruct`。
- 训练链：SFT（冷启动）→ GRPO（RL），RL 从 SFT 续训、不跳过。
- Prompt 体系：数据集制作 prompt 与训练/推理 actor prompt 必须分离并独立版本化。数据集制作 prompt 可见媒体、`human_label`、完整 taxonomy、详细边界说明和候选生成规则，用于产生 `candidate_*`；它不得作为 actor 输入。actor prompt 使用一个 canonical 文本模板，包含紧凑 taxonomy（完整 risk_type/evidence 枚举、每类简短定义、映射和空集规则）与四字段 JSON schema，但严禁出现 `human_label`、`candidate_*`、`ref_*`、R5/advice/confidence、二元标签或 R5 内部规则。每条图片或 GIF 构成一个 actor prompt 实例，不为同一媒体制作多种问法或多轮对话；SFT、GRPO、评估和部署共用相同的 actor prompt 与 taxonomy schema 版本。分别记录 `dataset_prompt_version`、`actor_prompt_version`、`taxonomy_schema_version`。两套Prompt正文、渲染变量、SFT target和防泄漏检查见[数据集制作与训练Prompt规范.md](数据集制作与训练Prompt规范.md)。
- GRPO prompt 组：SFT 为每个 actor prompt 实例监督一个参考 completion；GRPO 为同一实例优先采样 `num_generations=4` 个 completion，并只在该 prompt 组内计算相对优势；若 smoke test 不能稳定运行，再降为 `2`。首轮固定 `generation_batch_size=8`，表示 8 张训练卡合计生成 8 个 completion，即 2 个 Prompt 实例 × 每个实例 4 个 completion，不表示存在多套 Prompt 模板。GRPO rollout 固定 `do_sample=true`、`temperature=0.9`、`top_p=0.95`、`top_k=-1`、`num_beams=1`、`repetition_penalty=1.0`；评估保持同一 actor prompt，但使用独立的确定性/低温生成配置，不复用训练 rollout 温度。
- 类别分布：类别不平衡在训练前通过定向数据收集解决；首版不设置基于 `human_label` 或 `risk_type` 的加权/平衡 sampler，也不设置类别相关 reward 倍率。训练对整理后的数据集使用常规随机打乱与采样。
- SFT 监督目标：正式 SFT 对完整四字段生产 JSON `{description, evidence, risk_type, cot}` 进行 assistant-only token loss 训练；system、user 与视觉 token 不计入 loss。GRPO 从该 SFT adapter 启动并继续生成相同的完整 JSON，只优化输出的正确性与一致性，不负责从零补学字段或输出格式。仅含 `evidence+risk_type` 或去掉 `cot` 的短输出方案不作为正式 actor。
- GRPO 奖励结构：采用“渐进格式分 + 严格语义门 + 分解任务奖励”。格式条件依次为严格 JSON object `J`、精确四键 `K`、字段类型/非空约束 `T`、枚举/空集规则 `E`、canonical key 顺序 `C`；格式分固定为 `r_fmt=0.25J+0.20JK+0.20JKT+0.25JKTE+0.10JKTEC`，语义门固定为 `g=JKTE`。只有 `g=1` 才开启 `risk_type`、`evidence`、R5 outcome 与 `cot` 奖励并调用 R5；canonical key 顺序错误只扣格式分，不关闭语义门。不得只使用单一 R5 命中奖励。正向奖励初始权重固定为 `format/type/evidence/R5/cot = 0.20/0.25/0.35/0.15/0.05`。`r_type` 对预测与参考 risk_type 做46类严格 exact-match，不给同一业务风险域部分分。`r_ev` 将预测与参考 evidence 分别转成集合后计算 set-F1：两边都为空取 1、仅一边为空取 0，其余取 `2*|intersection|/(|pred|+|ref|)`；数组内部顺序和重复项不影响该分数，首版不为 evidence 重复制定额外规则。非法 evidence 或不允许的空集合仍由语义门拦截。`r_R5` 仅在 `g=1` 时比较预测字段经锁定版本 R5 输出的 `vlm_advice` 是否等于 `human_label`；不使用 `vlm_confidence`、二元结果或 R5 内部路径，R5 基础设施异常须中止/跳过批次并报警，不得记为模型 0 分。`r_cot` 只检查 cot 与模型自身核心字段的机械自洽，公式固定为 `0.40*coverage + 0.20*no_extra + 0.20*type_mention + 0.20*order_ok`；空 evidence 时须先显式写出 `evidence=[]`，再确认 risk_type。首版不设置 description 在线语义奖励，其质量由完整 JSON SFT、非零 KL、字段约束与离线检查保持。软超长惩罚固定为 `-0.03*clip((L-L_soft)/(L_max-L_soft),0,1)`，其中 `L_soft` 取合法参考 JSON token 长度 P95，`L_max` 至少覆盖 P99并留余量。GRPO 不设置重复惩罚；重复率仅作为离线评估和训练监控指标，不参与 reward 计算，详见 `GRPO奖励方案对抗性调研.md`。
- 训练框架：SFT 与 GRPO 统一使用 MS-SWIFT 的标准 Transformers 后端（非 Megatron-SWIFT）与 DeepSpeed ZeRO-3；GRPO 由 8 个训练 rank 原地使用 Transformers rollout，显式设置 `use_vllm=false`、`ds3_gather_for_generation=false`。原生 Hugging Face `Transformers + TRL + PEFT + Accelerate/DeepSpeed` 仅在 MS-SWIFT 出现无法绕过的阻断性问题时作为备用实现。框架选择的完整调研依据见 `训练框架调研.md`，分布式细节见 `8卡分布式训练方案.md`。
- 参数策略：SFT 与 GRPO 均以 FP16 LoRA 为主，底座不量化；QLoRA 仅在 FP16 LoRA 经显存收缩后仍 OOM 时作为备用，全参数微调不进入实验矩阵。视觉编码器、multimodal merger/projector、token embedding 与 `lm_head` 全程冻结；LoRA 只注入语言解码器的 `q_proj/k_proj/v_proj/o_proj/gate_proj/up_proj/down_proj`。两阶段均使用 `lora_rank=32`、`lora_alpha=64`、`lora_bias=none`；SFT 使用 `lora_dropout=0.05`，GRPO 继续加载该 adapter，但显式设置 `disable_dropout=true`，使训练时 dropout 的有效值为 0。rank 降低不作为首要 OOM 手段。
- 输出契约：VLM 只输出一个 JSON 对象，canonical 字段顺序固定为 `{description, evidence, risk_type, cot}`。其中 `risk_type` 为单值、`evidence` 为多值数组、`cot` 为包含 1～3 个短步骤的字符串数组；risk_type 与 evidence 均采用封闭枚举，模型不得输出清单外值。evidence 数组内部顺序不承载语义，首版不要求去重，也不设置相关奖励、预处理或专项监控。除 `gamble_other`、`porn_other`、`politic_other`、`fraud_other` 允许 `evidence=[]` 外，其余 42 类至少输出一项 evidence。CoT 必须显式说明可观察内容对每个 evidence 的支持，再显式说明这些 evidence 对最终 risk_type 的支持，不得声称存在数组之外的 evidence；当 evidence 为空时，CoT 必须说明风险域依据和退回 `*_other` 的原因。关闭 Qwen 原生 thinking；SFT、GRPO、评估和部署不得在 JSON 前后输出 `<think>`、Markdown 或额外解释。项目定义的 `cot` 是可检查的 evidence→risk_type 简短确认，不等于模型原生长推理。R5 只消费 `{risk_type, evidence}`，并输出 `{vlm_advice, vlm_confidence}`。
- 媒体输入：静态图作为单个 image，由 Qwen3-VL 动态分辨率 processor 处理，`IMAGE_MAX_TOKEN_NUM=1024`，等价 `max_pixels=1048576`；不裁剪、不改变宽高比，超限时仅确定性等比例缩小。GIF 不超过4帧时全部使用，超过4帧时按累计播放时间的 `0、1/3、2/3、1` 位置抽取4帧；有序帧作为一个 video 帧列表输入，`VIDEO_MAX_TOKEN_NUM=256`/帧，等价 `max_pixels=262144`/帧，不得作为四张独立 image。数据制作、SFT、GRPO、评估和部署共用同一媒体实现与版本。
- 长度与 batch：SFT `per_device_train_batch_size=1`、`gradient_accumulation_steps=2`，有效 batch 16；GRPO `per_device_train_batch_size=1`、`gradient_accumulation_steps=1`、`generation_batch_size=8`。SFT/GRPO 均开启 `group_by_length`，但必须使用包含视觉 token 的真实 processor 编码长度；二者分别使用 target-inclusive 与 prompt-only length cache。禁用 packing 和 padding-free。GRPO completion 首轮硬上限为 512，正式 `L_soft/L_max` 由合法参考 completion 的 P95/P99 加余量确定。所有阶段使用 `truncation_strategy=delete`，不得静默截断 taxonomy、视觉 token 或四字段 JSON。
- V100 兼容：attention 首选 SDPA，不兼容时回退 eager；不使用 FlashAttention-2。默认纯 GPU ZeRO-3，CPU parameter offload 仅在完成峰值定位和 bucket 调整后仍 OOM 时启用；optimizer offload不是主要手段，首版排除 NVMe offload。两卡 LMDeploy PyTorchEngine TP=2 仅作为独立推理/数据制作/部署对照，不参与首版 GRPO。
- 验收范围：当前不定义冻结盲测集或 recall/precision 门槛；历史指标只作回归参考。当前交付目标是稳定产生经 R5 得到的二元结果与 10 类业务分类。
- 全程在 8×V100 上可复现、不 OOM。

## 最终需求状态

1. **模型选型（已确认）**：选择 `Qwen3-VL-8B-Instruct` 作为能力优先方案；4B 为已评估但未采用的资源优先替代方案，详见 `模型选型-4B与8B比较.md`。
2. **微调策略（已确认）**：使用 FP16 LoRA，QLoRA 仅作 OOM 备用，排除全参数微调；视觉编码器、multimodal merger/projector、token embedding 与 `lm_head` 全程冻结，LoRA 只注入语言解码器的7类主要线性层；SFT 后继续 GRPO。两阶段固定 `lora_rank=32`、`lora_alpha=64`、`lora_bias=none`；SFT 使用 `lora_dropout=0.05`，GRPO 显式设置 `disable_dropout=true`。rank 降低不作为首要 OOM 手段；普通训练超参数由实现侧依据 smoke test 与 dev 曲线决定。
3. **训练框架与分布式（已确认）**：SFT 与 GRPO 统一使用 MS-SWIFT 标准 Transformers 后端，不采用 Megatron-SWIFT；8张卡全部作为 ZeRO-3 训练 rank，GRPO 原地 Transformers rollout，不拆分专用 rollout 卡；显式设置 `use_vllm=false`、`ds3_gather_for_generation=false`。原生 Hugging Face 栈仅作阻断性故障备用。MS-SWIFT 底层仍可使用 Transformers、PEFT、TRL/DeepSpeed 等组件，这不等同于把裸 PEFT/TRL 作为项目主框架。详见 `训练框架调研.md` 与 `8卡分布式训练方案.md`。
4. **输出契约（已确认）**：以 `risk_type.md` 当前 46 类小类为封闭枚举，保留 `login_*`、`good_*` 等本身不违规或仅表示弱风险的类别；模型不得输出清单外类别，清单变更需显式版本升级。
5. **SFT 与 RL 各自目标（已确认）**：正式 SFT 使用完整四字段生产 JSON 做 assistant-only loss，GRPO 从该 adapter 接续并保持同一输出契约；不使用 description-only 或核心两字段模型作为正式 actor。训练/推理 actor prompt 与数据集制作 prompt 分离；首版 actor 只使用一个 canonical 文本模板，每条图片/GIF 是一个独立实例，标签、参考答案及 R5 信息均不可见，SFT、GRPO、评估和部署共用该模板。GRPO 为同一实例生成多个 completion。canonical 字段顺序固定为 `description → evidence → risk_type → cot`；关闭原生 thinking。字段最终 token 边界由正式 taxonomy 和参考数据统计确定，详见 `SFT输出方案调研.md`。
6. **RL 奖励设计（已确认）**：采用渐进格式分、严格语义门以及 `risk_type/evidence/R5/cot` 分解奖励，不使用单一 R5 outcome 作为全部奖励；正向奖励初始权重确认为 `0.20/0.25/0.35/0.15/0.05`，`r_fmt`/`g`、`r_type` exact-match、`r_ev` set-F1、`r_R5` advice exact-match 与 `r_cot` 四项自洽公式已确认。`vlm_confidence`、二元结果和 R5 内部路径不进入奖励，R5 基础设施异常须中止/跳过批次并报警，不得记为模型 0 分；首版不设置 description 在线语义奖励。软超长惩罚的 P95→`L_max` 线性公式与最大扣分0.03已确认；不设置重复惩罚，重复率只监控。reward scaling 固定使用 `scale_rewards=batch`。GRPO policy 从 SFT adapter 初始化，并以该初始状态的冻结快照作为 reference；KL 独立于 reward，固定 `kl_in_reward=false`、首版 `beta=0.04`。首版固定 `dynamic_sample=false`，组内奖励零方差只监控和归因，不自动丢弃并重采样。类别分布通过训练前的数据收集解决，不使用标签加权 sampler 或类别 reward 倍率。`num_generations=4`、`generation_batch_size=8` 为首轮固定值；rollout 固定 `do_sample=true`、`temperature=0.9`、`top_p=0.95`、`top_k=-1`、`num_beams=1`、`repetition_penalty=1.0`。详见 `GRPO奖励方案对抗性调研.md`。
7. **数据集制作与媒体输入（已确认，模型职责待 pilot）**：现有数据基础字段为图片、`md5` 标识、来源和 `human_label`，不另建复杂的原始媒体元数据层。静态图使用动态分辨率单 image 输入，超限时等比例缩小；GIF 最多输入4帧，不超过4帧时全部使用，超过4帧时按累计播放时间均匀选取4帧并包含首尾帧，有序帧作为一个 video 帧列表输入。静态图/GIF 每帧视觉预算分别为1024/256 token。数据制作 API、本地模型、SFT、GRPO、评估和推理使用同一媒体规则。媒体重复只使用现有 `md5` 识别，同一 `md5` 不得跨数据分区；首版不使用 pHash、视觉向量、模板近重复或 `source` 分组。`qwen3.7-plus`/agent 使用独立的数据集制作 prompt，参考媒体、`human_label`、完整 taxonomy、详细边界说明与候选生成规则，生成 `candidate_description/candidate_evidence/candidate_cot/candidate_risk_type`；该 prompt 不用于 actor 训练或部署。图片和 GIF 已获准发送至外部 API，因此首版不按媒体出域等级拆分标注路线；API 与本地模型的主生成、抽样对照及备用职责仍由同一 pilot 实测决定。每次生成仍须关联原记录的 `md5`，并记录精确模型快照、请求标识、`dataset_prompt_version`、`taxonomy_schema_version` 及原始响应。机器结果仅是候选。候选通过 JSON、封闭枚举、CoT 一致性及 R5 映射检查，并由数据负责人确认后，才能转为对应的 `ref_*` 训练标签；人工检查方式和覆盖率不属于本任务需求。
8. **数据量与类别占比（已确认）**：首版预算为门禁通过后的10,000条有效业务样本，参考四字段完成后按 `train:dev=90%:10%` 划分为约9000/1000条；同一 `md5` 不跨分区，并尽量保持10类 `human_label`、46类 `ref_risk_type`、媒体形态和困难样本分布，划分随机种子须固定并记录，SFT 与 GRPO 共用同一划分。首轮采集配额固定为 `human_label=good` 6,500条、其余9类合计3,500条；good 内部为 `good_clean=4,500`、`good_risk_signal=2,000`。2,000条 `good_risk_signal` 的采集方向固定为：porn相似但合规450、finance相似但合规400、gamble/game相似但合规300、login/客服/二维码/线路相似300、fake/官方/品牌相似但合规300、politic/军警/新闻相似但合规150、跨域多弱信号但仍为good 100；这些只是采集分层，若参考标注后实际构成非-good类别，必须移入对应配额，不得强行标为good。其余9类具体为 gamble 750、porn 700、finance 470、politic 460、other_fraud 360、login 320、fake 260、vpn 90、game 90。44个非-good `ref_risk_type` 每类至少60条有效且具内部多样性的样本；3,500条中最低覆盖使用2,640条，剩余860条按历史错误、业务频率、严重性和边界复杂度定向补充。困难/边界硬配额只固定上述2,000条 `good_risk_signal`；非-good困难正例不设500条总量或每类8条的硬要求，采集中自然遇到则保留并统计，不阻塞10k完成，训练后仅在学习曲线或分层诊断显示欠拟合时再定向补充。数据采用“两轮收集、自动盘点、只补缺口”：第一轮按10类配额收集常规业务数据，参考确认后自动统计46类覆盖，第二轮只补低于60条的risk_type和未完成的good混淆方向；第二轮期望补齐的risk_type/evidence不进入数据制作Prompt，原始收集量不写死。这里的 good/其余9类只是 `human_label` 采集桶，不预设 `risk_type` 到二元结果的固定映射，最终二元结果仍由R5决定。50/50不作为首版默认方案，因为它不等于10类或46类平衡、相对历史先验偏移过大且可能加剧误报；可在65/35和60/40仍不能解决尾类欠拟合时作为消融。10k是首版启动规模而非46类充分性的先验结论，须用固定final_dev上的2.5k/5k/7.5k/10k嵌套学习曲线判断是否继续定向扩量，详见 `数据规模与业务类别比例调研.md`。
9. **公开数据混合（已确认）**：首版 SFT 使用100%业务四字段数据建立基线，不直接混入公开原生 VQA、caption、通用聊天或纯文本指令数据；首版 GRPO 也不接收缺少完整 `risk_type/evidence/human_label/R5` reference 的公开原生样本。另建200～500条不参与训练的生产相关能力保持集，诊断中文 OCR、UI/文档、细节识别、GIF时序及四字段 JSON 稳定性，该集合不属于最终验收集。只有确认加载 adapter 后的生产相关能力退化，才进行5%与10%的定向 replay 消融；公开素材必须重新制作为同 actor prompt、同 taxonomy、同四字段 schema 的项目兼容数据并通过既有准入，比例同时按 assistant token 与 visual token 统计。详见 `公开数据混合与能力退化调研.md`。
10. **8卡运行时（已确认）**：SFT 有效 batch 16；GRPO 8卡原地 rollout、`generation_batch_size=8`。SDPA、`group_by_length`、真实视觉长度缓存和禁止截断为基线；纯 GPU ZeRO-3 优先，CPU parameter offload 仅作末级回退，详见 `8卡分布式训练方案.md`。
11. **实验后决策（有意保留）**：API 与本地标注模型职责、独立推理采用 Transformers 还是 LMDeploy、精确 token 上限、10k 后是否扩量以及普通训练超参数均由既定 pilot/统计/smoke/dev 结果决定，不在需求阶段伪造固定结论。
12. **验收（当前不讨论）**：本阶段不锁定盲测集、recall/precision 或相对提升门槛；只要求最终链路产生 R5 决定的二元结果与 10 类业务分类。

