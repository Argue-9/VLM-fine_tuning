# 公开数据混合与能力退化调研

> 研究对象：约 10k 条业务图像/GIF 数据，Qwen3-VL-8B-Instruct，LoRA SFT → GRPO，生产输出为固定四字段 JSON：`description`、`evidence`、`risk_type`、`cot`。
>
> 结论状态：推荐方案，可通过后述消融实验转为最终训练决策。

## 1. 结论先行

本项目**不应默认把公开通用多模态指令数据直接混入正式 SFT actor**，也不应把这类数据直接放入当前 GRPO。

推荐先用 100% 业务数据训练一个基线 actor，再用独立的“生产相关能力保持集”判断加载 LoRA adapter 后是否真的退化。只有确认退化时，才进行小比例、定向的 replay 消融。

若触发 replay，优先采用以下方式：

1. 从许可合格的公开来源选择能补充中文 OCR、界面/文档阅读、细粒度视觉理解或短时序理解的素材；
2. 不保留公开数据原生的 caption、VQA 或多轮对话答案；
3. 通过本项目的数据制作 prompt，重新生成并审核完整四字段 JSON；
4. 让这些样本使用与业务数据完全相同的 actor prompt、taxonomy 和输出 schema；
5. 先比较 0%、5%、10% 三档，比例按训练 token/视觉 token 统计，而不是只按样本条数统计。

因此，本调研对“是否需要公开数据”的回答是：

> **现在不需要预设必须混入。先测再加；需要加时，优先加入同任务、同 schema 的定向能力保持数据，而不是原样混入通用多模态问答。**

## 2. 问题边界

这里要避免把三类不同目标混为一谈：

| 目标 | 本项目是否需要 | 应如何处理 |
|---|---:|---|
| 保护未加载 adapter 的 Qwen3-VL-8B 底座 | 已由 LoRA 的冻结基座机制实现 | 保存原始底座和 adapter；无需为此混数据 |
| 保护“加载业务 adapter 后”的生产相关视觉能力 | 需要 | 建立能力保持集，实测 OCR、细节、GIF、JSON 等能力 |
| 保护通用聊天、百科问答等完整通用能力 | 当前未确认是产品要求 | 不默认优化；只有正式提出该要求时才考虑原生通用 replay |

LoRA 不改写底座参数，因此卸载 adapter 后仍可回到原始底座。但这不等于加载 adapter 后不会退化：语言解码层的低秩增量仍可能改变模型对视觉特征的解释方式、回答策略和格式偏好。

本项目冻结视觉编码器和 merger，只训练语言解码器 LoRA，这进一步降低了视觉表征本体漂移的风险，但不能保证 OCR、细节识别或时序推断的最终输出不变。

## 3. 研究证据与适用范围

### 3.1 领域微调确实可能造成遗忘

[Luo 等人的持续微调实验](https://arxiv.org/abs/2308.08747)在 1B–7B 文本模型上观察到领域知识、推理和阅读理解的遗忘，并发现加入一般指令数据可以缓解、但不能完全消除遗忘。

这项研究说明“遗忘风险存在”，但不能直接推出本项目必须混入公开数据，原因包括：

- 实验主要是文本 LLM，而本项目是多模态 VLM；
- 实验研究持续指令微调，本项目采用冻结底座的 LoRA；
- 该论文没有验证 Qwen3-VL-8B、四字段 JSON 或本项目风险 taxonomy；
- “缓解平均通用能力”不等于“改善业务分类与证据定位”。

因此，这是一项风险提示，不是混合比例的直接依据。

### 3.2 LoRA 通常比全量微调更少遗忘，但并非零遗忘

[《LoRA Learns Less and Forgets Less》](https://arxiv.org/abs/2405.09673)在代码和数学任务上比较了 LoRA 与全量微调。研究显示，标准低秩 LoRA 往往学习目标任务更少，但对域外能力的遗忘也更少；论文还指出 LoRA 的保持作用可强于 dropout、weight decay 等常见正则方式。

对本项目的含义是：

- 当前 LoRA 路线本身已经是一个较强的能力保持约束；
- 不能因为担心遗忘就无条件加入额外数据；
- 仍须测量“adapter 加载状态”下的能力，因为该论文没有证明 LoRA 不会遗忘；
- 学习率、目标模块和 rank 也会影响学习—遗忘权衡，公开数据不是唯一调节手段。

[另一项 LoRA 遗忘缩放研究](https://arxiv.org/abs/2401.05605)进一步报告：遗忘会随训练的参数量和更新步数增长，PEFT 也不能从机制上免疫灾难性遗忘。这支持“需要 probe”，但仍未给出适用于本项目 10k 多模态四字段数据的固定混合比例。

### 3.3 多任务数据既可能促进泛化，也可能产生负迁移

[FLAN](https://arxiv.org/abs/2109.01652)及后续的[大规模指令微调研究](https://arxiv.org/abs/2210.11416)表明，跨大量任务的指令微调可以提高对未见任务的零样本泛化。[Continual-T0](https://aclanthology.org/2022.emnlp-main.410/)也展示了在学习新任务的同时保持既有任务能力的可能性。

但这些结论来自以“成为通用指令模型”为目标的大规模、多任务训练，不能直接等价于本项目的窄任务 actor。生产 actor 只有一个规范任务：看图或 GIF，返回唯一、严格、固定顺序的四字段 JSON。

另一方面，[多任务梯度冲突研究](https://papers.nips.cc/paper/2020/hash/3fe78a8acf5fda99de95303940a2420c-Abstract.html)表明，不同任务的梯度可能相互干扰；[ForkMerge](https://papers.nips.cc/paper_files/paper/2023/hash/60f9118a849e8e9a0c67e2a36ad80ebf-Abstract-Conference.html)也直接研究了辅助任务造成目标任务性能下降的负迁移现象。

对本项目，公开原生任务与业务任务存在明显的目标差异：

- 公开数据常要求自由描述、VQA、知识问答或多轮对话；
- 业务数据要求固定 actor prompt 和固定 JSON schema；
- 公开答案通常没有项目的 46 类 `risk_type`、受控 `evidence`、R5 结果和 `human_label`；
- 通用回答通常追求完整自然语言，生产 actor 则要简短、可解析、受约束。

所以“数据更多”并不自动等于“目标更好”。直接混入可能同时增加泛化能力和格式/业务冲突，必须用消融实验判断净收益。

### 3.4 多模态与 RL 阶段的直接经验仍然只支持“可选工具”，不支持“默认处方”

[Continual Instruction Tuning for Large Multimodal Models](https://arxiv.org/abs/2311.16206)在连续视觉指令微调中观察到灾难性遗忘，并报告多任务联合训练与 replay 能缓解遗忘。这比纯文本研究更接近本项目，但训练任务、模型和输出协议仍不同。

[InstructBLIP](https://arxiv.org/abs/2305.06500)把大量视觉任务转成统一指令格式并进行平衡采样，说明经过协议统一的多源数据可以促进视觉任务泛化；它并不支持把异构原生输出不加处理地拼接起来。

[LLaVA-MoLE](https://arxiv.org/abs/2401.16160)则显示，直接把通用、文档和医学视觉数据交给普通 LoRA 混合训练，会让部分任务获益、部分任务下降，而且单纯提高 LoRA rank 不能消除冲突。这与本项目担心的 schema/任务负迁移高度相关。

[MM1](https://arxiv.org/abs/2403.09611)在其大规模多模态训练配方中有意识地混合视觉语言任务与 text-only 数据，以维持和扩展模型的指令能力；其结论来自大规模通用 MLLM 构建，而非 10k 领域 LoRA，因此只能证明“精心设计的数据混合可能有效”。

RL 阶段也有相似先例：[InstructGPT](https://arxiv.org/abs/2203.02155)在 PPO 阶段加入预训练梯度（PPO-ptx），缓解了若干公开 NLP 任务上的回归。但本项目当前 GRPO 的公开样本没有 R5/reference reward，不能照搬 PPO-ptx；若未来确实需要 RL replay，应单独设计可比较的辅助目标，而不是给无业务标签样本硬算当前 reward。

综上，现有一手证据同时说明三点：遗忘可能发生、replay 可能有效、朴素混合也可能负迁移。没有一项研究能证明本项目应默认混入公开数据。

## 4. 为什么不建议默认直接混入公开原生数据

### 4.1 输出协议冲突

正式 actor 的监督目标始终是：

```json
{
  "description": "...",
  "evidence": ["..."],
  "risk_type": "...",
  "cot": ["..."]
}
```

而公开数据的输出可能是：

- 一段自由图像描述；
- 一个短答案；
- 多轮 `human/assistant` 对话；
- 对偏好答案的 `chosen/rejected`；
- 英文的长篇视觉推理。

若在同一个 prompt 语义下监督不同格式，会形成直接矛盾。即使为公开样本配置另一套 prompt，模型仍在共享的 LoRA 参数上学习另一种回答协议，可能降低生产 prompt 的 JSON 合法率、键顺序稳定性、枚举闭集率和简洁性。

### 4.2 标签语义不可直接对齐

公开 caption/VQA 的“正确”只表示它回答了公开任务，并不表示：

- `risk_type` 符合项目 46 类定义；
- `evidence` 只包含可观察证据；
- `cot` 确认了 evidence → risk_type；
- `human_label` 与 R5 一致；
- 负样本符合项目中的 `good` 或 `good_risk_signal` 语义。

因此，不能仅做字段包装后就视为业务监督数据。若要成为正式 actor 数据，必须重新标注和审核语义。

### 4.3 当前 GRPO 奖励无法评价通用公开任务

当前 GRPO reward 依赖业务参考信息，包括 `risk_type`、`evidence`、`human_label` 和 R5 规则。公开原生 VQA/caption 没有这些元数据，无法获得有意义、可比较的奖励。

强行混入会产生两种坏结果：

1. 给公开样本缺省奖励：不同任务的 reward 不可比，组内优势估计失真；
2. 只给格式奖励：模型可能学会 JSON 外形，却没有业务分类和 evidence 的正确约束。

因此，**公开原生样本不得进入当前 GRPO**。只有被完整重标、通过参考标注确认与 R5 一致性门禁，并拥有全部 reward reference 的样本，才可作为与业务样本同类的数据进入 GRPO。

### 4.4 8×V100 的 token 机会成本

公开多模态数据通常包含高分辨率图像、长描述、多图或视频。对 8×V100 16GB、FP16、Transformers 路线而言，额外视觉 token 会直接增加：

- 训练时长；
- 激活显存压力；
- padding 浪费；
- 长样本对有效 batch 的挤压；
- 对业务样本的更新比例稀释。

如果总 step 固定，加入 10% 公开数据就可能减少约 10% 的业务更新机会；这会把“公开 replay 本身的效果”和“业务曝光变少”混在一起。因此不能只比较固定总 step 的结果。

## 5. 正确的决策流程

### 5.1 先训练 100% 业务数据基线

基线记为 `A0`：

```text
SFT: 100% 业务四字段数据
GRPO: 100% 业务可奖励数据
公开数据: 0%
```

这是必要对照。没有 `A0`，就无法判断公开数据是防止退化，还是只是改变了优化结果。

### 5.2 建立三套相互独立的评估

#### A. 业务核心评估集

沿用正式业务 dev，关注：

- `risk_type` 分类结果；
- `evidence` 正确性；
- 四字段 JSON parse/schema/canonical-key-order；
- `cot` 是否只做 evidence → risk_type 的简短确认；
- 最终二元结果由 R5 计算，不要求 VLM 学固定映射。

#### B. 生产相关能力保持集

建议先建设 200–500 条、与训练集隔离的诊断集，覆盖：

| 能力 | 建议测试内容 | 原因 |
|---|---|---|
| 中文 OCR | 小字、低对比度、密集文字、价格/联系方式 | 风险判断常依赖画面文字 |
| 页面/UI 理解 | 网页、聊天界面、海报、按钮、弹窗 | 业务媒体可能不是自然图片 |
| 细粒度视觉 | 局部物体、符号、人物动作、遮挡 | 防止分类正确但 evidence 虚构 |
| 长文本整合 | 多区域文字与视觉的联合判断 | 检查解码器是否忽略局部信息 |
| GIF/短时序 | 关键帧变化、前后动作、短暂出现内容 | 静态首帧无法覆盖此能力 |
| 指令/JSON | 固定 prompt、严格 schema、闭集枚举 | 这是生产协议的一部分 |

该集合可以包含不进入训练的自有样本，也可包含许可允许的公开素材。其目标不是代表所有通用能力，而是覆盖业务生产链真正依赖的底座能力。

该集合只用于诊断能力退化，不属于本项目当前暂不讨论的最终验收集。

#### C. 可选的通用能力评估集

只有当“加载业务 adapter 后仍要作为通用多模态助手”被确认为产品要求时，才增加通用 VQA、caption、知识问答等评估。否则广泛基准下降不应自动触发 replay。

### 5.3 定义退化，而不是凭感觉判断

对每项能力 (k)，计算：

\[
\Delta_k = M_k(\text{adapter loaded}) - M_k(\text{base})
\]

但必须区分两种 prompt：

- 业务任务用正式 actor prompt 比较 base、SFT、GRPO；
- 通用能力用该能力的原生、固定 prompt 比较 base 与加载 adapter 后的模型。

建议在实验前锁定触发条件，例如满足任一项才进入 replay 消融：

- 生产相关能力指标稳定下降超过 3 个百分点；
- bootstrap 置信区间整体低于预先设定的非劣界；
- OCR、细节或 GIF 能力的下降可重复，并会实际改变 `risk_type`/`evidence`；
- GRPO 相比 SFT 出现明显能力回退，且不能由格式解析或随机波动解释。

“3 个百分点”只是首轮工程阈值，不是论文给出的普适常数。最终应结合样本量和业务代价调整。

## 6. 触发退化后的优先方案

### 6.1 方案 A：同协议、定向能力保持 replay（推荐）

从公开来源筛选对生产有用的视觉素材，然后通过项目数据制作链路重标：

```text
公开媒体
  → 许可与去重门禁
  → 项目数据制作 prompt
  → 四字段候选 JSON
  → schema/枚举/可见性校验
  → human_label 与 R5 一致性门禁
  → SFT replay 候选
```

优点：

- actor 始终看到同一个任务 prompt；
- 输出始终是同一个四字段 schema；
- 增加视觉/OCR/时序多样性时，不引入另一套回答协议；
- 通过完整门禁后，样本也具备进入 GRPO 的可能性；
- 训练和评估口径与业务数据一致。

风险：

- 公开素材可能大量被标为 `good`/`good_risk_signal`，改变类别分布；
- teacher 可能对陌生公开域产生系统性伪标签；
- 重标会产生额外 API 与参考标注确认成本；
- 公开数据的授权条款仍然沿用，重标不会消除原始媒体许可义务。

控制措施：

- 只选与生产能力缺口相关的来源，不追求“覆盖所有公开数据”；
- 按风险类别、媒体类型、OCR 密度和时序难度分层抽样；
- 公开数据不用于补齐业务类别配额，除非它确实通过同样的语义门禁；
- 单独保留 `source_dataset`、`source_id`、许可和版本信息；
- 业务 train/dev 与公开候选按现有 `md5` 做完全重复隔离；沿用项目已确认的首版策略，不增加感知 hash 或文本近重复流程。

### 6.2 方案 B：原生通用 instruction replay（仅条件性备选）

只有当产品明确要求保留通用助手能力时，才考虑保留公开数据原生 prompt/answer，并为它设置独立 `task_id` 与模板。

建议从 2%–5% 的 SFT assistant token 起步，且：

- 不能让同一 prompt 对应两套输出 schema；
- 必须持续监控业务 JSON 合法率和业务分类/evidence；
- 不进入当前 GRPO；
- 若通用任务与业务任务明显冲突，应停止混合，而不是继续加大比例；
- 若通用能力确属产品目标，可能需要独立 adapter 或路由，而不是让单一 adapter 兼顾所有协议。

本项目当前不推荐方案 B 作为首版。

## 7. SFT 与 GRPO 的具体放置方式

### 7.1 SFT 阶段

推荐的首轮消融：

| 实验 | 业务数据 | 同协议公开重标数据 | 原生公开数据 | 用途 |
|---|---:|---:|---:|---|
| A0 | 100% | 0% | 0% | 必做基线 |
| A1 | 95% | 5% | 0% | 推荐第一档 replay |
| A2 | 90% | 10% | 0% | 仅在 A1 有正收益后验证 |
| B1 | 95% | 0% | 5% | 仅当通用能力是明确需求 |

这里的比例优先定义为：

\[
p_{replay}=
\frac{T_{assistant,replay}}
{T_{assistant,business}+T_{assistant,replay}}
\]

同时记录视觉 token 比例：

\[
p_{visual}=
\frac{T_{visual,replay}}
{T_{visual,business}+T_{visual,replay}}
\]

不能只用记录数，因为一条多帧 GIF 或长 caption 的计算量可能远大于一张短文本图片。

为了公平评估，建议进行两类对照：

1. **业务曝光量固定**：A0、A1、A2 的业务样本更新次数相同，replay 增加额外 5%/10% 计算；用于判断 replay 是否真正有益；
2. **总 token 预算固定**：所有实验总 token 相同；用于判断在现实算力预算下是否值得牺牲业务曝光。

在 8×V100 约束下，先做短程 A0/A1；只有 A1 同时改善保持集且不伤业务集时，再运行 A2 或完整训练。

### 7.2 GRPO 阶段

正式 GRPO 保持业务单任务：

```text
GRPO actor prompt: 仅正式业务 prompt
GRPO samples: 仅具备完整 reference 与 R5 门禁的数据
KL reference: 冻结的 SFT actor 初始状态
公开原生数据: 0%
```

已有非零 KL 能抑制 GRPO actor 远离 SFT 起点，但它只能保持 SFT 已经具备的行为。如果 SFT 已经因错误混合而降低 JSON 稳定性，KL 会把这种状态也锚定下来。因此数据混合决策应主要在 SFT 阶段完成。

若能力在 SFT 后正常、GRPO 后下降，优先依次检查：

1. reward 是否鼓励牺牲描述/evidence 以换取分类分；
2. KL 系数和实际 KL 曲线是否异常；
3. rollout 长度、截断和解析失败是否造成伪退化；
4. 是否应早停或回退 GRPO checkpoint。

不应首先用无法评分的公开问答填入 GRPO。

## 8. 公开数据的准入规则

任一公开数据在下载或训练前必须通过以下门禁：

1. **用途许可**：许可允许项目预期用途；若未来可能商用，非商业许可数据不得进入正式训练。
2. **媒体权利**：不能只看标注 JSON 的 license；还要核验底层图像/视频的原始许可和再分发条件。
3. **来源可追溯**：保存数据集名、版本/commit、原始 ID、URL、许可快照和下载日期。
4. **隐私与合规**：排除个人敏感信息、无授权人脸、医疗/身份文件等不合规内容。
5. **评估数据隔离**：任何用于能力保持诊断的公开 benchmark 样本和业务 dev 不得回流训练。
6. **完全重复清理**：按项目现有 `md5` 规则与业务 train/dev、能力保持集和历史回归集做完全重复隔离；首版不扩展近重复检测。
7. **模态匹配**：优先图像、UI/文档和短时序；不因“多模态”名义混入大量无关纯文本。
8. **能力相关**：每个来源必须对应一个已观察到的生产能力缺口。
9. **schema 兼容**：进入方案 A 的数据必须重新生成完整四字段并通过同一门禁。
10. **资源可控**：限制分辨率、帧数、文本长度，并记录预处理后的视觉 token。
11. **语言适配**：英文 caption 数据不能被默认视为中文 OCR 保持数据。
12. **版本冻结**：消融期间锁定具体数据版本，避免滚动数据导致结果不可复现。

## 9. 候选公开数据集评估

以下仅是候选来源，不代表已经通过本项目法律与内容准入审查。

### 9.1 M3IT

[M3IT 原始论文](https://arxiv.org/abs/2306.04387)介绍了由 40 个数据集组成、约 240 万实例、400 条人工任务指令的多模态多语言指令数据，并将部分任务扩展到 80 种语言。

适用点：

- 多语言和中文能力比纯英文数据更相关；
- 任务和图像来源较多，可用于筛选 OCR、文档、图表等能力素材；
- 可作为生产相关能力保持集或方案 A 的素材池。

限制：

- 原生输出不是本项目四字段；
- 由多个底层数据集聚合而成，必须逐子集核验许可；
- “支持中文指令”不等于图像中包含高质量中文 OCR；
- 不能整体无差别混入。

结论：**条件性候选，适合按子集筛选和重标，不适合原样全量混入。**

### 9.2 LLaVA-Instruct-150K

[官方 Hugging Face 数据仓库](https://huggingface.co/datasets/liuhaotian/LLaVA-Instruct-150K)包含 conversation、detail description 和 complex reasoning 等英文视觉指令数据，数据卡标注为 CC BY 4.0。

适用点：

- 详细描述和复杂视觉推理可用作细粒度视觉素材候选；
- 规模适中，容易抽取小比例样本做消融。

限制：

- 主要是英文自由回答；
- 对中文 OCR 和 GIF 时序帮助有限；
- 底层图像来源和许可仍应单独核验，不能只依赖聚合仓库标签；
- 原生 GPT 生成答案与本项目 taxonomy 无直接对应关系。

结论：**低优先级条件性候选，只建议筛素材并重标。**

### 9.3 LLaVA-OneVision 训练数据

[LLaVA-OneVision 官方说明](https://github.com/LLaVA-VL/LLaVA-NeXT/blob/main/docs/LLaVA_OneVision.md)显示其训练数据覆盖单图、多图、视频、文档/图表/屏幕、OCR、数学/推理等类别。

适用点：

- 模态覆盖最接近“静态图像 + GIF/短视频”的能力保持需求；
- 文档、屏幕和 OCR 子集可能对业务更有价值；
- 可用于构建能力保持集的来源清单。

限制：

- 规模大、来源复杂，许可需按子集审查；
- 原生任务/对话格式与业务 actor 冲突；
- 多图/视频样本的视觉 token 对 V100 成本高；
- 不能把 benchmark 测试数据用作训练 replay。

结论：**能力覆盖强，但只能按子集、按缺口使用；不建议整体混入。**

### 9.4 RLAIF-V

[RLAIF-V 官方仓库](https://github.com/RLHF-V/RLAIF-V)说明其包含 83,132 对多模态偏好数据，重点是减少幻觉和提高可信度；数据集采用 CC BY-NC 4.0，并明确限制为研究用途。

适用点：

- 可用于研究视觉幻觉、错误细节和回答可信度；
- 更适合作为离线诊断材料或另行研究偏好训练。

限制：

- `chosen/rejected` 偏好结构不是本项目 SFT 四字段 reference；
- 非商业限制可能与未来落地冲突；
- 英文为主；
- 当前 GRPO reward 不支持这种偏好样本。

结论：**不进入正式训练候选；最多用于研究性诊断。**

## 10. 推荐的最小实验矩阵

考虑 8×V100 的成本，首轮不需要把所有组合全部跑满。

### 阶段一：确认是否存在问题

1. 训练 A0 短程 SFT；
2. 比较 base 与 A0 的生产相关能力保持集；
3. 比较 A0 的业务 dev、JSON、分类和 evidence；
4. 若无实质退化，停止公开数据混合实验，继续正式 SFT → GRPO。

### 阶段二：只在退化时验证 replay

1. 从退化能力对应来源中筛选定向公开素材；
2. 重标为同协议四字段；
3. 运行 A1（5%）；
4. 只有 A1 在保持集改善且业务非劣时，再运行 A2（10%）；
5. 若 A1 无收益或伤害 schema/业务指标，停止 replay，不继续扩大比例。

### 阶段三：验证 GRPO 影响

对胜出的 SFT checkpoint 运行相同 GRPO 配置，然后比较：

```text
SFT checkpoint
  vs.
SFT → GRPO checkpoint
```

同时看业务指标、能力保持指标和 KL 曲线。GRPO 的公开数据比例保持 0%，除非公开素材已经完成项目全套重标与 reward reference 门禁。

## 11. 实验控制与记录要求

为避免把混合效果和其他变量混淆，每组实验应固定：

- 同一 base model 与 tokenizer/processor 版本；
- 同一 LoRA target、rank、alpha、dropout；
- 同一图像缩放和 GIF 抽帧策略；
- 同一最大输入/输出长度；
- 同一学习率、scheduler、warmup 和有效 batch；
- 同一业务 train/dev；
- 同一 actor prompt 和 taxonomy 版本；
- 同一评估解码参数；
- 相同或明确标注的 seed。

每次记录：

```text
run_id
base_model_revision
adapter_config
business_sample_count / assistant_tokens / visual_tokens
replay_source / version / license
replay_sample_count / assistant_tokens / visual_tokens
business exposure count
total optimizer steps
peak GPU memory
wall-clock time
business metrics
retention metrics
JSON/schema metrics
GRPO KL statistics（若适用）
```

若预算允许，对决定性短程实验至少使用 2 个 seed；若只能单 seed，则应报告 bootstrap 区间并避免把小幅波动解释为确定收益。

## 12. 决策表

| 观察结果 | 决策 |
|---|---|
| A0 业务好，生产相关能力无明显退化 | 不混公开数据，继续正式训练 |
| A0 仅通用聊天下降，但产品不需要通用聊天 | 不混；该下降不构成业务阻塞 |
| A0 中文 OCR/UI/GIF 等生产能力可重复下降 | 做 5% 同协议定向 replay A1 |
| A1 保持能力改善，业务与 schema 非劣 | 再验证 10% A2；择最小有效比例 |
| A1 改善通用能力但伤业务分类或 JSON | 拒绝该来源/格式，不扩大比例 |
| A1 无效 | 优先调学习率、训练时长、LoRA target/rank 或样本质量，不机械加数据 |
| SFT 正常、GRPO 后退化 | 调 GRPO reward/KL/早停；不直接加入无法评分的公开任务 |
| 产品明确要求一个 adapter 同时做通用助手 | 单独研究多任务模板、原生 replay 或 adapter 路由，不沿用当前默认方案 |

## 13. 最终推荐

本项目的正式首版方案为：

```text
SFT:
  100% 业务四字段 JSON
  公开原生数据 0%

GRPO:
  100% 具备项目 reference 与 R5 门禁的数据
  公开原生数据 0%

评估:
  业务核心集
  + 200–500 条生产相关能力保持集

触发 replay 后:
  优先公开素材重标成同 prompt、同 schema 的项目数据
  先 5%，再视结果验证 10%
  比例同时按 assistant token 和 visual token 报告
```

这一路线接受“能力退化可能存在”的研究证据，但不把公开数据混合作为未经验证的固定处方。它把训练目标限定为生产真正需要的能力，并通过可复现实验决定 replay 是否有净收益。

## 14. 主要来源

- Luo et al., [An Empirical Study of Catastrophic Forgetting in Large Language Models During Continual Fine-tuning](https://arxiv.org/abs/2308.08747)
- Biderman et al., [LoRA Learns Less and Forgets Less](https://arxiv.org/abs/2405.09673)
- Kalajdzievski, [Scaling Laws for Forgetting When Fine-Tuning Large Language Models](https://arxiv.org/abs/2401.05605)
- Wei et al., [Finetuned Language Models Are Zero-Shot Learners](https://arxiv.org/abs/2109.01652)
- Chung et al., [Scaling Instruction-Finetuned Language Models](https://arxiv.org/abs/2210.11416)
- Scialom et al., [Fine-tuned Language Models are Continual Learners](https://aclanthology.org/2022.emnlp-main.410/)
- Yu et al., [Gradient Surgery for Multi-Task Learning](https://papers.nips.cc/paper/2020/hash/3fe78a8acf5fda99de95303940a2420c-Abstract.html)
- Jiang et al., [ForkMerge: Mitigating Negative Transfer in Auxiliary-Task Learning](https://papers.nips.cc/paper_files/paper/2023/hash/60f9118a849e8e9a0c67e2a36ad80ebf-Abstract-Conference.html)
- He et al., [Continual Instruction Tuning for Large Multimodal Models](https://arxiv.org/abs/2311.16206)
- Dai et al., [InstructBLIP: Towards General-purpose Vision-Language Models with Instruction Tuning](https://arxiv.org/abs/2305.06500)
- Chen et al., [LLaVA-MoLE: Sparse Mixture of LoRA Experts for Mitigating Data Conflicts in Instruction Finetuning MLLMs](https://arxiv.org/abs/2401.16160)
- McKinzie et al., [MM1: Methods, Analysis & Insights from Multimodal LLM Pre-training](https://arxiv.org/abs/2403.09611)
- Ouyang et al., [Training language models to follow instructions with human feedback](https://arxiv.org/abs/2203.02155)
- Li et al., [M3IT: A Large-Scale Dataset towards Multi-Modal Multilingual Instruction Tuning](https://arxiv.org/abs/2306.04387)
- LLaVA-VL, [LLaVA-OneVision official data and training documentation](https://github.com/LLaVA-VL/LLaVA-NeXT/blob/main/docs/LLaVA_OneVision.md)
- LLaVA, [LLaVA-Instruct-150K official dataset repository](https://huggingface.co/datasets/liuhaotian/LLaVA-Instruct-150K)
- RLAIF-V, [official repository and license notice](https://github.com/RLHF-V/RLAIF-V)
