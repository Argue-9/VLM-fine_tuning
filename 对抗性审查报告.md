# 规格与验收文档及工作分解对抗性审查报告

- 审查日期：2026-08-06（2026-08-07 按反馈调整：暂缓数据分布/数量/收集/验收簇）
- 审查基准（真值来源）：`任务需求说明.md`
- 审查对象：
  - `规格与验收文档.md`（已确认基线规格）
  - `.scratch/qwen3-vl-content-review/issues/`（18 个 `ready-for-agent` issue）
- 审查方法：逐条比对 需求 → 规格 → issue，重点查找 **遗漏 / 漂移（软化或硬化）/ 伪造已延迟决策** 三类问题。所有"丢弃/无命中"结论均经 `grep` 在规格与全部 18 个 issue 中复核。
- 审查立场：对抗性——主动尝试推翻"规格已覆盖该需求"的假设，对每条结论要求可定位证据（行号 + 原文）。
- 范围调整：按反馈，**数据集分布/数量/收集/验收簇（原 F1、F2、F7、F9、F10、F11、F14）暂缓**——该部分将根据实际情况灵活调整，不作为本次硬性发现跟踪。下文"当前关注发现"仅保留契约/配置/治理/Prompt/溯源类问题。

---

## 目录

- [总体结论](#总体结论)
- [优点（忠实落地项）](#优点忠实落地项)
- [发现汇总表](#发现汇总表)
- [当前关注发现](#当前关注发现)
  - [F3. 变更控制与"禁止未记录手工修复"有规格、无 issue 归属、无验收条件](#f3-变更控制与禁止未记录手工修复有规格无-issue-归属无验收条件)
  - [F4. 无任何 issue 负责创建/锁定数据制作 prompt](#f4-无任何-issue-负责创建锁定数据制作-prompt)
  - [F5. 零方差组"只监控归因、不自动丢弃重采样"被丢弃](#f5-零方差组只监控归因不自动丢弃重采样被丢弃)
  - [F6. 每次生成元数据未进入 AC-B12](#f6-每次生成元数据未进入-ac-b12)
  - [F8. actor prompt 丢失"每类简短定义/映射"](#f8-actor-prompt-丢失每类简短定义映射)
  - [F12. evidence 去重"专项监控"排除被截断](#f12-evidence-去重专项监控排除被截断)
  - [F13. issue 使用 `Qwen3-VL-8B` 丢失 `-Instruct`](#f13-issue-使用-qwen3-vl-8b-丢失-instruct)
- [暂缓部分（数据分布/数量/收集/验收，按实际情况灵活调整）](#暂缓部分数据分布数量收集验收按实际情况灵活调整)
- [建议修复（仅当前关注项）](#建议修复仅当前关注项)
- [审查方法与复核附录](#审查方法与复核附录)

---

## 总体结论

规格与 issue 整体**高度忠实**于需求。在契约落地与"不伪造延迟决策"两方面纪律严明。按反馈暂缓数据分布/数量/收集/验收簇后，当前关注问题共 **2 中、2 中低、3 低**（2026-08-07 已全部修复，详见"当前关注发现"段状态横幅），集中在两处：

1. **工作分解的归属与可验收性缺口**：变更控制/禁止手工修复、数据制作 prompt 产出归属、零方差不丢弃、每条生成元数据——这些规格条款没有对应的 issue 归属或验收条件。
2. **轻微忠实性漂移**：actor prompt 内容清单、evidence 去重监控排除、模型名精度。

---

## 优点（忠实落地项）

下列需求区域经逐条核对为忠实落地（仅列举，不展开）：

- **奖励结构**：`r_fmt = 0.25J+0.20JK+0.20JKT+0.25JKTE+0.10JKTEC`、`g=JKTE`、正向权重 `0.20/0.25/0.35/0.15/0.05`、`r_type` exact-match、`r_ev` set-F1（双空=1/单空=0/部分交集）、`r_cot = 0.40coverage+0.20no_extra+0.20type_mention+0.20order_ok`、空 evidence 先写 `evidence=[]` 再确认 risk_type、软超长 `-0.03*clip((L-L_soft)/(L_max-L_soft),0,1)` 与 `L_soft=P95`/`L_max=P99+余量`。
- **GRPO 配置**：`beta=0.04`、`kl_in_reward=false`、`scale_rewards=batch`、`dynamic_sample=false`、`num_generations=4`/`generation_batch_size=8`、rollout `do_sample=true/temp=0.9/top_p=0.95/top_k=-1/num_beams=1/repetition_penalty=1.0`、SFT adapter 初始化 + 冻结快照 reference、G=4 不稳降 G=2 且总 completion 仍为 8。
- **媒体管线**：静态图单 image + `IMAGE_MAX_TOKEN_NUM=1024`（≡`max_pixels=1048576`）+ 等比例缩小不裁剪；GIF ≤4 帧全用、>4 帧按累计播放时间 `0、1/3、2/3、1` 位置（≡含首尾均匀）取 4 帧、作为有序 video 帧列表、`VIDEO_MAX_TOKEN_NUM=256`/帧（≡`262144`）；数据制作/SFT/GRPO/评估/部署同一媒体实现。
- **Prompt 分离**：数据制作 prompt 与 actor prompt 独立正文/版本；actor prompt 禁见 `human_label`/`candidate_*`/`ref_*`/R5/advice/confidence/二元标签/R5 内部规则；每媒体一个 actor 实例；记录三类版本。
- **训练框架与参数**：MS-SWIFT 标准 Transformers 后端 + DeepSpeed ZeRO-3、`use_vllm=false`/`ds3_gather_for_generation=false`、8 rank 原地 rollout；FP16 LoRA、rank32/alpha64/bias-none、SFT dropout0.05 + GRPO `disable_dropout=true`；冻结视觉塔/merger/projector/embedding/`lm_head`，LoRA 仅注入 `q/k/v/o/gate/up/down_proj`；SFT 有效 batch 16（bs1×ga2×8）、GRPO bs1×ga1×`gen_bs=8`；target-inclusive / prompt-only 真实视觉长度 `group_by_length`；禁 packing/padding-free/FlashAttention-2；`truncation_strategy=delete`。
- **输出契约**：单 JSON、canonical 顺序 `{description,evidence,risk_type,cot}`、四类 `*_other` 允许 `evidence=[]`、cot 1~3 步、关闭原生 thinking、禁 `imd`/Markdown/JSON 外文本；R5 只消费 `{risk_type,evidence}`。
- **不伪造延迟决策**：学习率/epoch/scheduler/warmup/早停交由 smoke/dev（规格 7.11）；API↔本地、Transformers↔LMDeploy 保留为试点结果；`L_soft/L_max` 由 P95/P99 派生。
- **不伪造数值门槛**：明确不设 precision/recall/F1 盲测门槛，2214 条仅作回归（AC-F10、Out of Scope #3、`规格与验收文档.md:398`）。

---

## 发现汇总表

### 当前关注（7 条）

| # | 级别 | 主题 | 需求行 | 规格/issue 位置 |
|---|------|------|--------|-----------------|
| F3 | 中 | 变更控制/禁止手工修复有规格、无 issue、无 AC | `任务需求说明.md:67` | `规格与验收文档.md:251,252` |
| F4 | 中 | 无 issue 产出/锁定数据制作 prompt | `任务需求说明.md:48,70` | `规格与验收文档.md:169,170,175,352,344`；`issue 03,05:10,06` |
| F5 | 中低 | 零方差组不丢弃/不重采样保障丢失 | `任务需求说明.md:69` | `规格与验收文档.md:205,372`；`issue 13,14,16` |
| F6 | 中低 | 每条生成元数据未进 AC-B12 | `任务需求说明.md:70` | `规格与验收文档.md:344`；`issue 05:11,06` |
| F8 | 低 | actor prompt 丢"每类简短定义/映射" | `任务需求说明.md:48` | `规格与验收文档.md:172` |
| F12 | 低 | evidence 去重"专项监控"排除被截断 | `任务需求说明.md:55` | `规格与验收文档.md:135` |
| F13 | 低 | issue 用 `Qwen3-VL-8B` 丢 `-Instruct` | `任务需求说明.md:46` | `issue 12,14,15,16` |

### 暂缓（7 条，数据分布/数量/收集/验收簇）

| # | 原级别 | 主题 | 暂缓原因 |
|---|--------|------|----------|
| F1 | 中高 | `good_risk_signal` 硬化 + 重分类保障丢弃 | 数据配额/采集，按实际调整 |
| F2 | 中 | 两轮收集 + 第二轮反泄漏规则丢弃 | 采集流程，按实际调整 |
| F7 | 中低 | replay 双 token 比例丢弃 + 5%与→或 | 公开数据混合比例，按实际调整 |
| F9 | 低 | 50/50 条件消融逃生丢弃 | good/risk 分布比例，按实际调整 |
| F10 | 低 | 44 类≥60 丢"具内部多样性" | 数据量/采集质量，按实际调整 |
| F11 | 低 | 860 补充标准收窄 | 采集补充标准，按实际调整 |
| F14 | 低 | "嵌套"→"嵌套或明确可比较" | 学习曲线数据子集结构，按实际调整 |

> 暂缓项的详细证据见文末"暂缓部分"。其中 **F2 的反泄漏原则**（不把待补类目喂给数据制作 prompt）与具体数量无关，建议无论最终采用何种采集流程都保留该原则——见 F2 条目备注。

---

## 当前关注发现

> ✅ **状态（2026-08-07）：以下 7 条已全部修复。**
> - F12 → `规格与验收文档.md:135` 补全"也不设置相关奖励、预处理或专项监控"。
> - F8 → `:172` actor prompt 补"每类简短定义""risk_type 与 evidence 的映射关系"。
> - F5 → AC-E02（`:372`）补"组内奖励零方差只监控和归因，不自动丢弃并重采样"。
> - F6 → AC-B12（`:344`）补"md5、精确模型快照、请求标识、原始响应"。
> - F3 → 新增 AC-F11/AC-F12（`:397-398`）并更新验收引用至 AC-A01～AC-F12（`:400`）。
> - F4 → `issue 03` 扩展产出/锁定数据制作 prompt，同步 `issue 04/05/10/11/17` 的 03 引用。
> - F13 → `issue 12/14/15/16` 模型名统一为 `Qwen3-VL-8B-Instruct`。

### F3. 变更控制与"禁止未记录手工修复"有规格、无 issue 归属、无验收条件

- **规格原文**（`规格与验收文档.md:251-252`）：
  > taxonomy、字段 schema、媒体采样语义、R5 接口或 canonical Prompt 的任何变化都必须显式升级版本，并重新运行对应契约与回归测试。
  > 不得用未记录的手工修复替换候选生成、数据过滤或奖励结果。

- **需求对照**（`任务需求说明.md:67`）仅窄义要求"清单变更需显式版本升级"。规格此处全枚举是对需求的**合理扩展**，**不属伪造**。

- **复核**：`grep "手工修复|契约与回归|升级版本|版本升级|重新运行"` 在全部 issue 仅命中 `issue 06`"对同一冻结输入重新运行验证"（不同语义——重跑校验，非变更触发回归）。无 issue 负责执行"变更→升级→回归"流程；**无对应 AC**——AC-F07（`:393`）只要求**记录**版本，不强制**变更触发回归**；亦无 AC 禁止未记录手工修复。

- **判定**：规格 FAITHFUL，但 issue/AC DROPPED（工作分解与可验收性缺口）。

- **风险**：两条关键治理规则只写在规格"Implementation Decisions"，未落到工作分解与验收门槛，存在被跳过的可能。

### F4. 无任何 issue 负责创建/锁定数据制作 prompt

- **需求原文**（`任务需求说明.md:48,70`）：`dataset_prompt_version` 为一等独立版本化制品；数据制作 prompt 是独立正文，可见媒体、`human_label`、完整 taxonomy、详细边界与候选生成规则，**不用于 actor 训练或部署**。

- **规格**（`规格与验收文档.md:169,170,175`，AC-C05 `:352`，AC-B12 `:344`）：规定其独立正文与版本号、可追溯。

- **复核**：`issue 03`（"锁定 taxonomy、四字段 Schema 与 canonical actor Prompt"）的 checkbox **全部**围绕 actor prompt、evidence 枚举、cot 规则与 actor 侧防泄漏——**未产出数据制作 prompt**。`issue 05:10`"使用同一数据制作 Prompt"、`issue 06` 直接消费该 prompt，**均假定其已存在**。`grep "数据制作 Prompt|dataset_prompt"` 在 issue 仅命中 05 的消费性引用，无产出/锁定归属。

- **判定**：规格 FAITHFUL，issue DROPPED（归属缺口）。

- **风险**：pilot（`issue 05`）的前置依赖一个无人产出的制品；若数据制作 prompt 在 pilot 前未锁定版本，05 的同集比较与后续 `ref_*` 谱系可追溯性都建立在未冻结输入上。

### F5. 零方差组"只监控归因、不自动丢弃重采样"被丢弃

- **需求原文**（`任务需求说明.md:69`）：
  > 首版固定 `dynamic_sample=false`，组内奖励零方差**只监控和归因，不自动丢弃并重采样**。

- **规格**（`规格与验收文档.md:205`，AC-E02 `:372`）：仅 `dynamic_sample=false`。`grep "不自动丢弃|自动丢弃|重采样|只监控和归因|监控和归因|零方差"` 在规格**无命中**。

- **issue**（13/14/16）：仅"记录零方差组比例"。`grep "归因|丢弃|重采样|resample"` 在全部 issue **无命中**。

- **判定**：DROPPED。需求把"不自动丢弃并重采样"作为**显式规则**，而非 `dynamic_sample=false` 的必然推论（实现方仍可设 `dynamic_sample=false` 却自定义零方差丢弃）。规格丢显式禁止，issue 丢"归因"与"不丢弃重采样"两重保障。

- **风险**：零方差组处理口径不一致；若实现方自定义丢弃，将偏离需求"只监控归因"的口径。

### F6. 每次生成元数据未进入 AC-B12

- **需求原文**（`任务需求说明.md:70`）：
  > 每次生成仍须关联原记录的 `md5`，并记录**精确模型快照、请求标识**、`dataset_prompt_version`、`taxonomy_schema_version` 及**原始响应**。

- **规格 AC-B12**（`规格与验收文档.md:344`）：
  > 每条样本可追溯 `dataset_prompt_version`、`actor_prompt_version`、`taxonomy_schema_version`、R5 版本和媒体管线版本。

  **丢失**：精确模型快照、请求标识、原始响应。

- **缓解**：`issue 05:11`"保留原始响应、解析状态、四字段 candidate、失败原因和完整版本元数据"、`issue 06`"保留…生成后端和数据制作版本"部分覆盖，但 AC 更窄，验收可在不记录模型快照/请求标识时通过。

- **判定**：AC 漂移（收窄）。issue 部分缓解，但 AC-B12 未强制完整集合。

- **风险**：每条 `ref_*`/candidate 的可审计性与问题批次回溯弱于需求要求。

### F8. actor prompt 丢失"每类简短定义/映射"

- **需求原文**（`任务需求说明.md:48`）：
  > actor prompt 使用一个 canonical 文本模板，包含紧凑 taxonomy（完整 risk_type/evidence 枚举、**每类简短定义、映射**和空集规则）与四字段 JSON schema。

- **规格漂移**（`规格与验收文档.md:172`）：
  > actor Prompt 只包含任务说明、紧凑但完整的 taxonomy、四字段 JSON schema、枚举与空集合规则。

  丢"每类简短定义""映射"；"紧凑但完整"语义模糊（未明确要求每类定义与 evidence↔risk_type 对应关系）。

- **判定**：DROPPED（低）。注：evidence↔risk_type 对应关系由规格 2.10 转引至"锁定版本 taxonomy"，部分覆盖"映射"意图；但"每类简短定义"无着落。

### F12. evidence 去重"专项监控"排除被截断

- **需求原文**（`任务需求说明.md:55`）：
  > evidence 数组内部顺序不承载语义，首版不要求去重，**也不设置相关奖励、预处理或专项监控**。

- **规格漂移**（`规格与验收文档.md:135`）：
  > `evidence` 是字符串数组，只允许 taxonomy 定义的 evidence 值；数组顺序不承载语义，首版不要求去重。

  句号截断于"首版不要求去重。"，丢"也不设置相关奖励、预处理或专项监控"。

- **复核**："相关奖励"由 set-F1（9.6）+ 无重复惩罚（9.11）+ Out of Scope #6 间接覆盖；`grep "专项监控"` 在规格**无命中**，"专项监控"排除未落地。issue 无相关条目。

- **判定**：DROPPED（低，仅"专项监控"排除）。核心意图（顺序无关、不去重、无重复奖励）由他处间接保留，且未额外增设监控，风险低。

### F13. issue 使用 `Qwen3-VL-8B` 丢失 `-Instruct`

- **需求/规格**：均为 `Qwen3-VL-8B-Instruct`（`任务需求说明.md:46`；`规格与验收文档.md:4,189,AC-D01`）。
- **issue 漂移**：`issue 12/14/15/16` 标题与依赖写作 `Qwen3-VL-8B`，丢 `-Instruct`（base 与 instruct 歧义）。
- **判定**：低。精度问题，但 `-Instruct` 是需求明选（区别于 base），issue 应保持一致。

---

## 暂缓部分（数据分布/数量/收集/验收，按实际情况灵活调整）

> 以下发现按反馈暂缓，不作为本次硬性跟踪项；列出备查，待实际采集/划分时再定。

- **F1（原中高）`good_risk_signal` 硬化 + 重分类保障丢弃**：需求 `任务需求说明.md:71` 把 2,000/450/… 定为"采集分层"并要求"实际构成非-good 者移入对应配额，不得强行标为 good"；规格 AC-B07（`:339`）/`issue 07:10-11` 硬化为精确终态验收数且丢该保障。
- **F2（原中）两轮收集 + 第二轮反泄漏规则丢弃**：需求 `任务需求说明.md:71`"两轮收集、自动盘点、只补缺口…第二轮期望补齐的risk_type/evidence不进入数据制作Prompt"。规格与全部 issue 均无。**备注**：其中"不把待补类目喂给数据制作 prompt"的反泄漏原则与具体数量无关，建议无论最终采集流程如何都保留。
- **F7（原中低）replay 双 token 比例丢弃 + 5%与→或**：需求 `任务需求说明.md:72`"5%与10%…比例同时按 assistant token 与 visual token 统计"；规格 `:164` 改"5% 或 10%"且丢双 token 比例。
- **F9（原低）50/50 条件消融逃生丢弃**：需求 `:71`"可在65/35和60/40仍不能解决尾类欠拟合时作为消融"；规格 `:162` 无条件"不采用 50/50"。
- **F10（原低）44 类≥60 丢"具内部多样性"**：需求 `:71`"每类至少60条有效且具内部多样性的样本"；规格 `:160` 丢"具内部多样性"。
- **F11（原低）860 补充标准收窄**：需求 `:71`"按历史错误、业务频率、严重性和边界复杂度定向补充"；规格 `:160` 改"按错误风险和业务优先级"，丢"严重性/边界复杂度"。
- **F14（原低）"嵌套"→"嵌套或明确可比较"**：需求 `:71`"嵌套学习曲线"；`issue 09` 放宽为"嵌套或明确可比较"。

---

## 建议修复（仅当前关注项）

> ✅ **以下 7 项已于 2026-08-07 全部落地（见"当前关注发现"段状态横幅的逐条位置）。** 下方原文保留作修复记录。

1. **F3**：新增 AC"taxonomy/schema/媒体采样/R5 接口/canonical prompt 任一变更→显式版本升级 + 重跑契约与回归测试"，并加 AC"不得用未记录手工修复替换候选生成/数据过滤/奖励结果"。
2. **F4**：把 `issue 03` 扩为"锁定 taxonomy、schema、**actor prompt 与数据制作 prompt**"，或新增一条 issue 专门产出/版本锁定数据制作 prompt，置于 `issue 05` 之前。
3. **F5**：AC-E02 旁补"零方差组只监控归因、不自动丢弃重采样"显式条款。
4. **F6**：AC-B12 补"模型快照、请求标识、原始响应"三项。
5. **F8**：`规格与验收文档.md:172` 补"每类简短定义"与"映射（evidence↔risk_type 对应）"。
6. **F12**：`规格与验收文档.md:135` 句末补"也不设置相关奖励、预处理或专项监控"。
7. **F13**：`issue 12/14/15/16` 标题与依赖统一为 `Qwen3-VL-8B-Instruct`。

> 暂缓项（F1/F2/F7/F9/F10/F11/F14）的修复待实际采集/划分方案确定后再处理；其中 F2 的反泄漏原则建议无条件保留。

---

## 审查方法与复核附录

### 复核命令记录

- 规格侧反泄漏/重分类/两轮规则检索（`任务需求说明.md:71` 对照）：
  ```
  grep -n "强行标为good\|移入对应配额\|采集分层\|两轮\|只补缺口\|不进入数据制作\|原始收集量\|具内部多样性\|严重性和边界复杂度\|50/50\|65/35\|60/40" 任务需求说明.md   # 需求命中 :71
  grep -n 同上 规格与验收文档.md   # 仅命中 :78（user story 23）与 :162（§4.8 50/50），两轮/重分类/采集分层等全部缺失
  ```
- 规格是否引用研究文档（判定"是否转引致他处"）：
  ```
  grep -n "详见\|调研.md\|方案.md\|规范.md" 规格与验收文档.md   # 仅命中 :153（§4 标题），无任何转引
  ```
  → 规格自包含，未将数据采集流程细节转引至研究文档；两轮/重分类/反泄漏仅存在于 `任务需求说明.md`，规格与 issue 均无。
- issue 侧补强复核：
  ```
  grep -rn "两轮\|只补缺口\|补低于\|补齐\|移入\|强行标\|采集分层\|内部多样性\|边界复杂度\|65/35\|60/40\|assistant token\|visual token\|请求标识\|模型快照\|手工修复\|版本升级\|专项监控" .scratch/qwen3-vl-content-review/issues/
  # 仅命中 issue 08:14"不通过修改真实证据强行补齐"（不同概念）
  ```
- 规格关键行原文确认：
  - `规格与验收文档.md:135` → "数组顺序不承载语义，首版不要求去重。"（确认句号截断）
  - `规格与验收文档.md:172` → actor prompt 内容清单（确认丢"每类简短定义/映射"）
  - `规格与验收文档.md:251-252` → 变更控制 + 禁止手工修复（确认规格有、issue/AC 无）
  - `规格与验收文档.md:344`（AC-B12）→ 仅列五项版本字段（确认丢模型快照/请求标识/原始响应）

### 对抗性复核

本报告每条"丢弃/漂移"结论均经过：① 需求原文定位（行号+引文）；② 规格原文定位（行号+引文）；③ issue 全量 `grep` 复核；④ 反驳尝试（"是否被转引至研究文档/是否由他处间接覆盖"）。仅保留经反驳后仍成立的发现；已被间接覆盖的（如 evidence 去重的"相关奖励"由 set-F1 + 无重复惩罚 + Out of Scope #6 覆盖）相应降级或并入相关条目。数据分布/数量/收集/验收簇按反馈暂缓，不参与当前修复优先级。
