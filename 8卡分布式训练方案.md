# 8 卡分布式训练方案

> 状态：已确认基线；普通训练超参数由实现侧依据 smoke test 与 dev 曲线调整  
> 适用范围：Qwen3-VL-8B-Instruct、LoRA、SFT → GRPO、8 × NVIDIA Tesla V100-SXM2-16GB  
> 调研与核对日期：2026-08-06

## 1. 结论摘要

在当前硬件与已选框架约束下，首版不把 8 张卡拆成“训练卡 + vLLM rollout 卡”，而采用以下基线：

| 阶段 | GPU 分配 | 分布式方式 | 生成后端 |
|---|---:|---|---|
| SFT | 8 张全部参与 | MS-SWIFT + DeepSpeed ZeRO-3，单卡单进程 | 不涉及 rollout |
| GRPO | 8 张全部参与 | MS-SWIFT + DeepSpeed ZeRO-3，单卡单进程 | Transformers，`use_vllm=false` |

GRPO 固定设置 `ds3_gather_for_generation=false`，让 policy 在 ZeRO-3 分片状态下完成生成。8B FP16 权重分成 8 份后，静态参数容量预计可被单卡承载；仍需 smoke test 验证视觉/语言激活、KV cache、逐层 all-gather、reference 计算和 CUDA/NCCL 工作区形成的动态峰值。关闭 gather 的代价是 rollout 明显变慢，但可避免在生成前把完整 policy 收集到单张 16GB V100。

此前讨论过的“6 张 trainer + 2 张外置 rollout”不作为首版。当前 stable vLLM 要求 NVIDIA GPU compute capability ≥ 7.5，而 V100 的 compute capability 为 7.0；LMDeploy PyTorchEngine 虽支持 V100、Qwen3-VL 和 TP=2 独立推理，但当前不能直接替换 MS-SWIFT GRPO rollout。首版明确设置 `use_vllm=false`，不依赖旧版 vLLM、非官方补丁或自定义 LMDeploy 权重同步胶水。

## 2. 前提与边界

### 2.1 已确认条件

- 基座模型：Qwen3-VL-8B-Instruct。
- 训练顺序：先 SFT，再 GRPO。
- 训练形式：FP16 非量化基座 + LoRA。
- 硬件：8 × Tesla V100-SXM2-16GB；V100 无原生 BF16。
- 框架：MS-SWIFT 标准 Transformers 后端，DeepSpeed ZeRO-3。
- SFT LoRA：`r=32`、`alpha=64`、`dropout=0.05`、不训练 bias。
- GRPO LoRA：继续使用 SFT adapter，`dropout=0.0`；冻结 SFT 初始策略作为 KL reference。
- 冻结 ViT、aligner/projector、embedding、`lm_head`，只训练语言 decoder 中指定线性层的 LoRA。
- GRPO 首选 `num_generations=4`，测试不稳定后才考虑降为 `2`。
- 静态图作为单个 image，以 Qwen3-VL 动态分辨率完整输入；超过预算时仅由统一 processor 等比例缩小，不裁剪或改变宽高比。
- GIF 使用确定性四帧规则，并将有序四帧作为一个 video 帧列表输入，不能作为四张独立 image，也不能为了缓解 OOM 静默减少帧数。
- 静态图视觉预算为 `IMAGE_MAX_TOKEN_NUM=1024`，等价 `max_pixels=1048576`；GIF 为 `VIDEO_MAX_TOKEN_NUM=256`/帧，等价 `max_pixels=262144`/帧。

### 2.2 已确认的硬件拓扑与待实测信息

已确认 8 张 V100 位于同一台服务器，首版采用单机 8 进程、每卡 1 个进程、`world_size=8`，不设计跨机训练路径。是否存在 NVLink、NVSwitch 或仅 PCIe 互联，仍应以训练机执行 `nvidia-smi topo -m` 的结果为准；该结果影响 ZeRO-3 通信效率与性能预期，但不改变当前单机分布式拓扑。

## 3. 为什么选择 ZeRO-3，而不是普通 DDP 或 ZeRO-2

8B 参数按 FP16 粗略计算，仅权重就约占：

\[
8\times 10^9\times 2\ \text{bytes}\approx 16\ \text{GB}
\]

这还不包括视觉模块、激活值、CUDA 工作区、临时张量、LoRA 参数及其优化器状态。普通 DDP 会在每张卡上复制完整模型；ZeRO-2 会切分优化器状态和梯度，但仍在每个进程保留完整的 16-bit 模型参数。两者都无法为单张 16GB V100 留出可靠运行余量。

ZeRO-3 进一步切分 16-bit 模型参数，前向与反向时按层收集并再次切分，因此适合本项目。理想稳态下参数存储会分摊到 8 个 rank，但实际峰值显存不会简单等于总量的八分之一：视觉激活、长序列激活、通信 bucket、临时 all-gather 和内存碎片仍会形成峰值。

本项目不采用 DeepSpeed AutoTP 作为训练基线。当前 MS-SWIFT 文档说明 AutoTP 只支持全参数训练，并要求 ZeRO-0/1/2；它不符合本项目的 LoRA + ZeRO-3 组合。

## 4. SFT 的 8 卡方案

### 4.1 拓扑

```text
单机进程 0 ─ GPU 0 ┐
单机进程 1 ─ GPU 1 │
单机进程 2 ─ GPU 2 │
单机进程 3 ─ GPU 3 ├─ ZeRO-3 数据并行组，world_size=8
单机进程 4 ─ GPU 4 │
单机进程 5 ─ GPU 5 │
单机进程 6 ─ GPU 6 │
单机进程 7 ─ GPU 7 ┘
```

每个 rank 处理不同的训练样本，同时共同维护一个被 ZeRO-3 分片的模型。这里的“数据并行”不等于普通 DDP 完整复制模型；参数、梯度和优化器状态均由 ZeRO-3 管理。

### 4.2 已确认的 SFT batch

首版固定使用以下配置：

- `per_device_train_batch_size=1`
- `gradient_accumulation_steps=2`
- `world_size=8`

SFT 有效 batch size 为：

\[
B_{\text{SFT}}=1\times 2\times 8=16
\]

首版不把有效 batch size 直接增至 32。9,000 条训练数据在 batch 16 下每个 epoch 约有 563 次参数更新，而 batch 32 约为 282 次；batch 16 能在梯度稳定性和更新次数之间取得更合适的平衡。batch 32 只作为后续诊断对照：当 batch 16 的梯度或 loss 波动经确认过大时再测试。增加 gradient accumulation 通常不会解决单次前向产生的 OOM。

### 4.3 启动命令骨架

以下展示分布式、视觉预算和已确认 LoRA 边界；学习率、epoch 等普通训练超参数由实现侧依据 smoke test 与 dev 曲线调整。`<SFT_MAX_LENGTH>` 必须在正式 taxonomy 渲染和数据 token 统计后确定：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 \
NPROC_PER_NODE=8 \
IMAGE_MAX_TOKEN_NUM=1024 \
VIDEO_MAX_TOKEN_NUM=256 \
FPS_MAX_FRAMES=4 \
swift sft \
  --model Qwen/Qwen3-VL-8B-Instruct \
  --dataset <TRAIN_JSONL> \
  --val_dataset <DEV_JSONL> \
  --train_type lora \
  --torch_dtype float16 \
  --attn_impl sdpa \
  --freeze_vit true \
  --freeze_aligner true \
  --target_modules q_proj k_proj v_proj o_proj gate_proj up_proj down_proj \
  --lora_rank 32 \
  --lora_alpha 64 \
  --lora_dropout 0.05 \
  --per_device_train_batch_size 1 \
  --gradient_accumulation_steps 2 \
  --gradient_checkpointing true \
  --vit_gradient_checkpointing false \
  --group_by_length true \
  --average_tokens_across_devices true \
  --packing false \
  --padding_free false \
  --max_length <SFT_MAX_LENGTH> \
  --truncation_strategy delete \
  --deepspeed zero3
```

实施时还必须打印实际 `requires_grad=true` 的参数名，确认 LoRA 没有进入 ViT、aligner/projector、embedding 或 `lm_head`。V100 不使用 FlashAttention-2；首选 SDPA，若锁定版本不兼容再退回 eager。SFT 的 length cache 必须按“完整 actor prompt + 视觉 token + 四字段 target”的真实 processor 编码长度生成。

## 5. GRPO 的 8 卡方案

### 5.1 为什么 8 张卡都用于 trainer

在支持 vLLM 的新架构 GPU 上，可以把 GPU 拆成 trainer 与 rollout server 两组。但本项目的 V100 不在当前 stable vLLM 的官方支持范围内，所以首版不做 6+2 或 4+4 的资源切分。

8 个 rank 同时承担以下工作：

1. 以 ZeRO-3 分片方式加载和更新 policy。
2. 加载 SFT 初始策略对应的冻结 reference。
3. 通过 Transformers 完成 GRPO completion 生成。
4. 执行自定义 reward、KL 与 policy 更新。

这一方案的主要缺点是生成速度慢；主要优点是所有 8 张卡都能参与承载 policy/reference 相关状态，并且不依赖未受支持的 rollout 引擎。

### 5.2 `ds3_gather_for_generation=false`

MS-SWIFT 当前参数说明指出：

- `true`：生成前收集 policy 权重，生成更快，但需要单卡能够容纳收集后的模型。
- `false`：允许训练/生成大于单卡显存的模型，但生成更慢。
- `false` 与 vLLM 不兼容。

Qwen3-VL-8B 的 FP16 权重本身已经接近一张 16GB V100 的全部容量，因此首版必须优先测试：

```bash
--use_vllm false
--ds3_gather_for_generation false
```

不能依赖默认值，因为 `ds3_gather_for_generation` 当前默认是 `true`。

### 5.3 `generation_batch_size` 的计算

当前选择：

- 训练进程数：`num_processes=8`
- `per_device_train_batch_size=1`
- `num_generations=4`

MS-SWIFT 要求：

1. `generation_batch_size` 是 `num_processes × per_device_train_batch_size` 的整数倍；
2. `generation_batch_size` 能被 `num_generations` 整除。

因此最小合法值为：

\[
\operatorname{LCM}(8\times1,4)=8
\]

首轮固定使用：

```bash
--per_device_train_batch_size 1
--gradient_accumulation_steps 1
--num_generations 4
--generation_batch_size 8
```

这里的 `prompt` 指 **Prompt 实例**，不是不同的 Prompt 模板。项目始终只有一套 canonical Actor Prompt 模板；每条训练数据把该模板与本条静态图或 GIF video 帧列表组合成一个不同的 Prompt 实例。

一个 generation batch 包含 8 个 completion，即 2 个 Prompt 实例 × 每个实例 4 个 completion。`generation_batch_size` 与 `steps_per_generation` 不能同时显式设置，因此首版只设置前者。

如果将 `num_generations` 从 4 降为 2，在 8 个训练 rank、单卡 batch 为 1 的前提下，最小合法 `generation_batch_size` 仍然是 8，而不是 4；它会变成 4 个 Prompt 实例 × 每个实例 2 个 completion。因此，`4 → 2` 主要改变组内比较和候选多样性，并不保证降低总 completion batch 的显存峰值。

### 5.4 policy/reference 的正确指向

首次从 SFT 进入 GRPO 时应设置：

```bash
--adapters <SFT_ADAPTER_PATH>
--ref_adapters <SFT_ADAPTER_PATH>
```

含义是：可训练 policy 从 SFT adapter 初始化，KL reference 是该 SFT 初始策略的冻结快照。不能遗漏 `ref_adapters`，否则可能错误地比较 GRPO policy 与裸 base model。

从 GRPO checkpoint 续训时，应区分“可恢复训练状态”与“固定 reference”：

```bash
--resume_from_checkpoint <GRPO_CHECKPOINT>
--ref_adapters <SFT_ADAPTER_PATH>
```

reference 始终指向冻结的 SFT 初始策略，不随 GRPO checkpoint 更新。

### 5.5 启动命令骨架

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 \
NPROC_PER_NODE=8 \
IMAGE_MAX_TOKEN_NUM=1024 \
VIDEO_MAX_TOKEN_NUM=256 \
FPS_MAX_FRAMES=4 \
swift rlhf \
  --rlhf_type grpo \
  --model Qwen/Qwen3-VL-8B-Instruct \
  --dataset <TRAIN_JSONL> \
  --val_dataset <DEV_JSONL> \
  --adapters <SFT_ADAPTER_PATH> \
  --ref_adapters <SFT_ADAPTER_PATH> \
  --train_type lora \
  --torch_dtype float16 \
  --attn_impl sdpa \
  --freeze_vit true \
  --freeze_aligner true \
  --target_modules q_proj k_proj v_proj o_proj gate_proj up_proj down_proj \
  --lora_rank 32 \
  --lora_alpha 64 \
  --lora_dropout 0.0 \
  --disable_dropout true \
  --deepspeed zero3 \
  --gradient_checkpointing true \
  --vit_gradient_checkpointing false \
  --group_by_length true \
  --packing false \
  --padding_free false \
  --use_vllm false \
  --ds3_gather_for_generation false \
  --per_device_train_batch_size 1 \
  --gradient_accumulation_steps 1 \
  --num_generations 4 \
  --generation_batch_size 8 \
  --max_length <GRPO_PROMPT_MAX_LENGTH> \
  --max_completion_length 512 \
  --truncation_strategy delete \
  --beta 0.04
```

reward 和采样温度等参数沿用已确认的 GRPO 方案，此处不重复展开。正式脚本必须显式传入已确认的 reward 函数及权重，不能依赖示例默认奖励。GRPO 的 length cache 按“完整 actor prompt + 视觉 token”编码，不复用 SFT 的 target-inclusive 长度。`max_completion_length=512` 是首轮硬上限；数据完成后以合法参考 completion 的 P99 加余量校准，`L_soft` 取 P95。所有超长样本使用 `delete` 并统计，不得左截断 taxonomy 或截断四字段 JSON。

## 6. 显存与稳定性策略

### 6.1 默认启用

- FP16 与动态 loss scaling，并监控 NaN/Inf。
- DeepSpeed ZeRO-3。
- gradient checkpointing。
- SFT micro-batch=1；GRPO completion micro-batch=1。
- `attn_impl=sdpa`，不兼容时才回退 eager。
- `group_by_length=true`，且长度必须包含 processor 展开后的视觉 token；保持数据 shuffle。
- 静态图 `max_pixels=1048576`；GIF video 帧列表 `max_pixels=262144`/帧，最多四帧，少于四帧时不补造帧。
- completion 首轮上限 512；prompt 与 SFT 总长度在正式 taxonomy/数据编码后按实际分布确定。
- 记录每张卡的 allocated、reserved 与峰值显存，目标至少保留约 1GB 安全余量。

### 6.2 首轮不启用

- vLLM。
- FlashAttention-2。
- `torch.compile`、Liger、packing、padding-free 等附加优化。
- CPU parameter offload 与 optimizer offload。
- NVMe offload。
- QLoRA。

这些选项会引入新的兼容面或明显性能代价，应在稳定基线失败后逐项验证，不能一次性叠加。

### 6.3 OOM 降级顺序

1. 确认视觉预算确实生效，检查并隔离异常长/异常编码样本，定位 OOM 发生在加载、视觉前向、反向还是 rollout。
2. 校准 prompt/completion 上限并优先降低 completion 最大长度；不得截断 taxonomy、JSON，或在既定“最多四帧、少于四帧全部保留”规则之外进一步减帧。
3. 调整 ZeRO-3 communication bucket/prefetch 配置并重新记录逐卡峰值。
4. 调整 GRPO rollout/训练 batch；注意在 8 rank 配置下，`num_generations=2` 不会自动把最小 `generation_batch_size` 降到 4。
5. 仍然 OOM 时才启用 CPU parameter offload，并记录吞吐下降与主机内存/锁页内存占用；optimizer offload不是主要显存手段。
6. 首版不使用 NVMe offload；上述仍不稳定时才评估 QLoRA，并重新执行完整 smoke test。

四帧规则是数据、训练、评估和部署的一致性契约。若保持四帧后仍无法运行，应重新讨论并显式修改需求，而不是只在训练脚本中减少帧数。

## 7. Smoke test 与通过标准

### 7.1 环境与拓扑

- `nvidia-smi -L` 能看到 8 张 V100。
- `nvidia-smi topo -m` 被归档，明确 GPU 间 NVLink/PCIe 拓扑。
- 记录驱动、CUDA、PyTorch、Transformers、DeepSpeed、MS-SWIFT 与 NCCL 精确版本。
- 确认实际 attention backend 为 SDPA 可用 kernel 或已记录的 eager 回退；未加载 FlashAttention-2、未启动 vLLM。

### 7.2 SFT smoke test

- 使用 8～16 条包含单图、少于四帧GIF和恰好四帧GIF video帧列表的真实格式样本，并覆盖视觉上限附近的样本。
- 8 卡 ZeRO-3、micro-batch=1，至少完成 2 个 optimizer step。
- 所有 GPU 均有计算负载；无 OOM、NaN/Inf、NCCL hang。
- 打印并核对全部可训练参数，仅目标 decoder LoRA 为可训练状态。
- 记录 step time、samples/s、各卡峰值显存和通信等待时间。

### 7.3 GRPO smoke test

- 从保存并重载成功的 SFT adapter 启动。
- 使用真实 actor prompt、真实最多四帧的video输入和合法四字段 JSON 输出长度；原生 thinking 关闭，输出不得带 `<think>` 前后缀。
- `num_generations=4`、`generation_batch_size=8`，至少完成 2 次参数更新。
- 验证 policy 与 reference 指向正确，reference 不随更新变化。
- reward plugin 能正确处理合法输出、缺字段、非法枚举、R5 正负反馈。
- 无单卡完整 8B 模型聚合导致的 OOM；各卡保留约 1GB 安全余量。

只有 smoke test 通过后，才能把本文中的“推荐起始值”升级为固定训练配置，并锁定完整环境版本。

## 8. 备选方案与启用条件

| 方案 | 何时启用 | 主要问题 |
|---|---|---|
| 8 卡 ZeRO-3 + Transformers | 当前首版 | rollout 慢，但兼容边界最清晰 |
| 两卡 LMDeploy PyTorchEngine TP=2 | 独立推理、数据制作或部署对照 | 支持 V100/Qwen3-VL，但当前不能直接接入 MS-SWIFT GRPO rollout；首轮 `vision_batch_size=1` |
| 6 卡 trainer + 2 卡外置 rollout | 原生 rollout 吞吐不可接受且完成定制连接器、权重同步与 6 卡 trainer 显存验证后 | `num_generations=4` 时最小合法 `generation_batch_size` 将变为 12；工程复杂度高 |
| 历史 vLLM 固定版本 PoC | 仅作实验性兼容验证 | 当前 stable 不支持 V100；旧版本与当前 Qwen3-VL/MS-SWIFT 组合无维护保证 |
| ZeRO-3 CPU parameter offload | 纯 GPU 方案在完成峰值定位与 bucket 调整后仍 OOM | CPU 内存、PCIe 带宽和训练速度代价较大；不启用 NVMe offload |
| 原生 Transformers + TRL/PEFT | MS-SWIFT adapter/reference 或 ZeRO-3 generation 出现无法绕过的问题时 | 需要自行维护更多训练胶水代码 |

## 9. 已确认配置与实验后决策

已确认：

1. GRPO 首轮固定 `generation_batch_size=8`，不把 16 纳入首轮实验。
2. 静态图视觉预算固定为 1024 token；GIF 最多四帧、256 token/帧，并使用单个 video 帧列表。
3. SFT 与 GRPO 开启 `group_by_length`；禁用 packing 与 padding-free。
4. completion 首轮上限 512；所有阶段禁止静默截断。
5. 默认纯 GPU ZeRO-3；CPU parameter offload 仅作末级回退；首版排除 NVMe offload。
6. 独立推理对照为 Transformers 手动均衡与两卡 LMDeploy PyTorchEngine TP=2；它不改变 GRPO 的 8 卡原地 rollout。

实验后确定：

1. 正式 taxonomy 渲染和数据编码后的 prompt/SFT 总长度硬上限，以及 completion P95/P99 对应的 `L_soft/L_max`。
2. Transformers 与 LMDeploy 在独立推理、数据制作或部署中的最终职责。
3. 普通训练超参数；由实现侧根据 smoke test、dev 曲线、数值稳定性和吞吐自行选择，不改变本文架构约束。

## 10. 官方来源

- [MS-SWIFT 命令行参数：DeepSpeed、batch、GRPO、reference 与 generation](https://swift.readthedocs.io/en/v3.12/Instruction/Command-line-parameters.html)
- [MS-SWIFT GRPO 文档](https://swift.readthedocs.io/en/v3.12/Instruction/GRPO/GetStarted/GRPO.html)
- [MS-SWIFT Qwen3-VL Best Practice](https://swift.readthedocs.io/en/v3.12/BestPractices/Qwen3-VL-Best-Practice.html)
- [DeepSpeed ZeRO：Stage 1/2/3 的切分范围](https://www.deepspeed.ai/tutorials/zero/)
- [vLLM stable GPU 安装要求：NVIDIA compute capability ≥ 7.5](https://docs.vllm.ai/en/stable/getting_started/installation/gpu/)
- [NVIDIA：V100 compute capability 为 7.0](https://developer.nvidia.com/blog/nvidia-ampere-architecture-in-depth/)
- [FlashAttention CUDA 支持范围：FA2 需要 Ampere/Ada/Hopper](https://github.com/Dao-AILab/flash-attention#nvidia-cuda-support)
- [Hugging Face Accelerate：`balanced_low_0` 与 `max_memory`](https://huggingface.co/docs/accelerate/main/concept_guides/big_model_inference)
- [LMDeploy 安装与 Volta/V100 支持](https://lmdeploy.readthedocs.io/en/stable/get_started/installation.html)
- [LMDeploy Qwen3-VL 模型支持表](https://lmdeploy.readthedocs.io/en/stable/supported_models/supported_models.html)
- [项目训练框架调研](训练框架调研.md)
